# memory.md — Controltable 專案進度記憶

> **這份檔案留的是「為什麼這樣做、踩過哪些坑、還沒做完什麼」。**
> 欄位語意的權威來源是 `FIELD_SPEC.md`；不可違反的鐵律在 `CLAUDE.md`；資料庫綱要在 `DB_table.md`。
> 三者重複的內容這裡一律不抄，只留 **它們沒寫的判斷理由與量測證據**。
>
> 2026-09-05 整理過一次：把「同一批被記三遍」「每批的逐條驗證清單」「歷次 `?v=` 值」
> 「已作廢的環境快照」「已劃掉的待辦」清掉，**3065 行 → 506 行**（142.8k → 27.0k 字元）。
> **⚠️ 的坑從 209 處收斂成 163 處，少掉的全部是重複**（例如「截圖拿不到」原本每批各寫一次，
> 現在集中在第 5 節）。**使用者原話、實測數字、被否決過的做法、未完成事項一條都沒有刪。**

---

## 0. 目前狀態

| 項目 | 值（2026-09-05 第 55 批當天實測） |
|---|---|
| 本機 DB 資料 | `dbo.Controltable` **62 筆** active（16 進行中 / 46 已完成；使用者 2026-09-12 21:40 重灌過一次，稽核列與計數欄當時全部歸零）；`dbo.Assignee` **13 筆**，`EMPO` 與 `EMAIL` 使用者已全部補齊（EMS 8 / MSD 5，例：明翰 `Macgyver_Ho@UMCG`、宸詳 `Sw_Lee@UMCG`） |
| 未壓日期 | **4 筆**：NID 6、20（皆 stage 4）、99（stage 2）、111（stage 1）。需關注 13 件（逾期 9 + 未壓 4 + 7 日內 0） |
| 已執行腳本 | `01`~`15`、`17`、`18`、`19` 已執行（`18`/`19` 於 2026-09-21 第 74/75 批：瀏覽權限四張表）。**`16_grant_dbmail_permission.sql` 尚未執行**（見第 4 節） |
| `?v=` | **`20260921002`**（第 75 批；第 62~64 批的結論記在 `CLAUDE.md`，這裡沒有另寫一份） |
| 瀏覽權限 | 本機：開關 **false**、規則 **1 筆**（使用者自己加的 `DEPT_3=IMD`）、`dbo.AccessAdmins` **3 筆**（`00002732`／`00041817`／`yu-tinglin`，由 `19` 種入）、`AccessLog` 7 筆。`appsettings.json` 的 `Access:Admins` 已改回 **空清單**（後備用），Development.json **不放**。⚠️ 模擬名冊 `[WEB].[dbo].[notes_person]`（34 筆）**裡面有 `EMPNO = 'yu-tinglin'`（林玉婷，DEPT_3=MSD）** —— 拿這台機器測「不在名冊」那條路會過不了，要用 `?testEmpId=` 拿別的工號測 |
| 伺服器 | `dotnet run` → `http://localhost:5146`（`.claude/launch.json` 的 `controltable`，用 `preview_start` 啟動）。使用者自己那台可能開在 **7127**，兩邊資料不同 —— 找不到某筆測試資料時先確認是哪一台 |
| DB 連線 | `sqlcmd -S Sariel -d Controltable -U testuser -P test -C -f 65001` |
| 寄信 | **`Mail:Mode = "smtp"` + relay `10.13.2.221:25`，`From` 刻意留空**，已由使用者在公司 IIS（p58esiap12）實測通過。⚠️ 見 `CLAUDE.md` 那條「未經指示不要動」 |

⚠️ **「需關注」的件數會自己變** —— 預警窗是 `DUE_WINDOW_DEFAULT = 7` 天，日期往前滾一天就會有需求進窗。
數字對不上時先用 `buildDueList(data, DUE_WINDOW_DEFAULT)` 印 `diffDays` 再判斷，**別急著當成 bug**。
歷史值：2026-08-23 = 4 件、08-24 = 9 件、08-27 = 10 件、09-05 = 13 件。

⚠️ **已知的資料狀態，不是 bug，別急著「修」**：
- 有階段跳空的資料（例：NID 49 `StageCode=5`、有 `UatEnd` 但 `MsdEnd` 空）。gating 與
  `StagePrereqViolations` 都刻意只在「值真的被改動時」才驗，就是為了不讓這些列變成「有值卻永遠改不動」。
- 部分 `UpdatedAt` 停在測試當下的時間（空存檔留下的），不是資料被改壞。
- `NotesLink` **62/64 是空的**；`Status` 與 `StatusID` 有 **58/64 格可互相推出**（只有 stage 1 那 6 筆帶新資訊）。
- DB 裡有一筆 `IsDeleted=1` 的 **ZZTEST26**（第 26 批的測試殘留，軟刪除所以不會出現在畫面上）。
- **指派人員維護視窗（`renderAssigneeModal()`）目前在 UI 上進不去** —— 入口鈕於 2026-08-18 依使用者要求移除
  （「目前用不到」），名單由他直接在 SSMS 的 `dbo.Assignee` 維護。程式與 `/api/assignees` 都還在，
  **日後要恢復入口只要把按鈕加回來**（`onClick` 設 `setIsAssigneeModalOpen(true)`）。
- **匯入那條路（TRUNCATE + 交易）從來沒有被端到端實測過** —— 測它要真的跑一次重灌，
  代價是使用者現有的 64 筆。要測請先備份。

---

## 1. 專案目的

EMS 與 MSD 跨部門的需求管控表。流程有順序：

```
① EMS 提供 Spec → ② MSD 確認沒問題 → ③ MSD 壓開發日期 → ④ EMS 驗收 → 結案
```

EMS 也可以先壓預設的驗收時間，或等開發完再填。

**高階主管的核心需求**（設計時的第一優先）：
1. 清楚知道需求是**什麼時候建立**的
2. 規格填完後**是否被異動過**，有異動就要留下可追蹤的紀錄

---

## 2. 已拍板的決定（不用再問）

| 項目 | 決定 |
|---|---|
| `NID` | 初期**不自動產生流水號**，手動輸入（主管可能會調整編號規則） |
| 匯入 Excel | **維持 `TRUNCATE` 全部清空重灌**，是初期測試的刻意做法，功能穩定後整個移除。**不要改成 UPSERT** |
| `MpSaving` | 自由文字，**可能不是數字**（「3人天」「待評估」） |
| Excel 最後一欄 `Status` | 存的是**階段代號** `(1)`~`(5)`，不是 Init/Ongoing/Done |
| `MsdConfirm` | 拆成 `MsdConfirm DATE` + `MsdConfirmNote NVARCHAR(500)` |
| 角色權限 (EMS/MSD/主管) | **暫不實作**（誰能改哪筆需求仍然沒有模型）。⚠️ 與「頁面瀏覽權限」（第 74 批，誰**看得到**這個網頁）是兩件事，不要混 |
| 頁面瀏覽權限（第 74 批，2026-09-21） | 做法對齊 `C:Gantt`：`[WEB].[dbo].[notes_person]` 名冊 + `dbo.AccessRules`（AND／OR 規則）+ 開關預設 false。四個決定（都選了我建議的那一個）：①管理者一律可瀏覽（**第 75 批起清單在 `dbo.AccessAdmins`、面板維護；`appsettings` 的 `Access:Admins` 降為後備**）；②**工號後端自己讀**（不收 `?empId`）；③**只擋畫面**、API 維持匿名；④稽核另開 `dbo.AccessLog`。不要「對齊回」Gantt 的自選主管登入與前端帶工號 |
| `OverallStatus` | **三種**：`Init` / `Ongoing` / `Done`。⚠️ `Pending` 於 2026-08-22 依使用者要求移除（「暫時不需要此狀態」）。**他對這欄改過主意兩次（三種→四種→2026-08-22 又拿掉），不要自作主張加回來。** 舊值收斂成 `Ongoing` 不是 `Init`。**`Done ⇔ StatusID 5`（第 67 批 H5）**：兩欄不合併，但不可互相矛盾 |
| `StatusID` (`StageCode`) | 純數字 `1`~`5`，**不含括號**，顯示在資料列上 |
| 刪除需求 | **軟刪除**（`IsDeleted = 1`）。已刪除的 NID 可以再被使用 |
| `NID` 唯一性 | 前後端都檢查，重複時跳**阻擋型視窗**（不是 toast） |
| 新增必填 | **`NID`、`MainCat`、`SubCat`、`EMS` 四欄**。`1_EMSStart`（2026-08-22）與 `1_EMSEnd`（2026-09-02 / 第 42 批）都已拿掉；`1_EMSEnd` 只在「原本就有值」時必填（擋手動清空） |
| 資料列不顯示「建立日」 | 與 `年月` 是同一個日期，完整建立時間放明細 |
| 資料列不顯示「現況描述」 | 長文塞進資料列只會被截成「1.因CMS WL…」，只在明細完整顯示 |
| `Remark` 是純文字不是網址 | DB 欄名 `NotesLink` 是早期誤解留下的舊名。**畫面上一律純文字，不可做成 `<a href>`**（2026-08-17 踩過） |
| ② MSD 確認Spec 沒有備註欄 | 只壓確認日期。`MsdConfirmNote` 保留、明細仍顯示既有值，不提供編輯 |
| ⭐ **Start 不重要，一切以 End 為準** | 2026-08-22 定調：判斷走到哪一階段**只看 End**；Start 沒填**自動帶成與 End 同一天**（三條寫入路徑都做）；**改 End 才算異動**（要理由、計 ⚠），**改 Start 不算**（只寫 `起日調整` 稽核列） |
| 異動理由的觸發 | **只有「End 真的被改掉」才強制**。解鎖但沒動 End、只動 Start、End 首次填寫，都不需要理由 |
| `End` ≥ `Start` | ①③④ 前後端都擋，同一天允許。② 只有單一日期不受限 |
| 註冊日期 | 資料列顯示 `RegDate`（`YYYY/MM/DD`）。`YearMonth` **不刪**，降級為由 `RegDate` 反推的衍生值，只留給 Excel 匯入匯出與趨勢圖分組 |
| 五階段名稱 | 1.EMS規格確認 / 2.MSD確認中 / 3.MSD開發中 / 4.EMS驗收 / 5.結案。**只改顯示文字，Excel 與 DB 欄名一律不動**（`1_EMSStart`、`2_MSDHistory` 是匯入對應表的 key） |
| History 存法 | **`dbo.Controltable_History` 稽核表**（第 13 批）。匯入本來就會清空 History，舊字串直接廢棄不遷移 |
| 異動人員 | Windows 帳號自動帶入（作法對齊 `C:Gantt`：`Negotiate` + `/api/whoami`）。取不到帳號 `ChangedBy` 留空但**不擋存檔**。⚠️ 帳號由前端帶回是可以偽造的，但本專案沒有權限模型，Gantt 也是同樣做法 |
| 模擬帳號 | `Auth:AllowSimulation`，開發開、正式關；模擬寫入的稽核列一定標 `simulated` |
| 規格回退的清空範圍 | 清空 **≥ 目標階段**的全部日期（含目標階段本身）。計數欄不清，那是既成事實。**目標可以是目前這一階段自己**（第 70 批，2026-09-12 使用者選的）：StatusID 不變、只清這一階（含）以後 —— 「重做 ③」不必再把走完的 ② 一起清掉。目標晚於目前仍 400 |
| 往回走的三條路（第 66 批） | 手動 StatusID **只能往前**；規格變了 → 規格回退；誤按標記完成 → 「撤銷」（`/undo-done`，只退一格、計數減回去、原訂日期與 RollbackCount 不動）。使用者 2026-09-11 選了「加撤銷按鈕」而不是「就用規格回退」。第 68 批起兩支都帶「畫面上看到的狀態」（`historyId` / `fromStage`）給後端比對，不符 409 |
| 完成日的下限（第 68 批） | **max(前一階段實際結束的那一天, 半年前)**；`Start` **不是下限、不可以拿回來**（存檔時 Start 自動 = End，拿它當下限等於「補登提早」做不到）。「前一階段實際結束的那一天」= max(原訂 End, ActualEnd)，提早／延期都套 |
| StatusID 不可空白（第 66 批） | `17_stagecode_not_null.sql`；匯入空白由 `InferStageCode()` 推一次寫進去。H4（匯入後對不變量跑檢查、列出違反的列）**使用者沒選、沒做** |
| 逾期判定 | **只有一份規則 `isPhasePassed()`**（第 23 批）。`resolveDuePhase()` = 排除走完的階段 → 剩下有日期的取**到期日最早**的。⚠️ 不可改回「四個日期一起比」；⚠️ 也不可加回「退回最後一個已排定的階段」；⚠️ **「走完了沒」只看 StatusID（第 65→66 批），不可再加回任何「下一階段有日期 → 這階段走完」的反推** |
| 逾期未壓 | **`unsetDuePhase()`**（第 33 批）：`StatusID` 走到某階段、那一階段自己沒日期 → `level='unset'`。入口統一 **`resolveFocusPhase()`**（先問未壓、沒有才退回 `resolveDuePhase()`）。⚠️ 不受 7 日窗限制；⚠️ `StageCode` 空白或超出 1~5 **一律不推斷**；⚠️ 判斷比 `e.level`，**不可拿 `diffDays` 反推**（`null <= 7` 在 JS 是 **true**） |

**這條資料鏈的四個關節（要動它們之前先看這裡）**：
1. **`init` 不算異動** —— 所有次數統計一律 `WHERE ChangeType <> 'init'`，否則每筆資料光是建立就掛上 ⚠1
2. **gating 只擋「從空白開始填寫」** —— 已有值的欄位照舊可解鎖修改，否則階段跳空的資料會有值卻永遠改不動
3. **延期完成時 End 不動、只寫 `ActualEnd`** —— 所以畫面一定要同時呈現「原訂 → 實際」
4. **三個計數欄是稽核表的 denormalized 快取** —— 徽章與排序直接讀欄位不 parse 稽核表；但**事實以稽核表為準**，不一致時以稽核表去修計數欄

**每批的固定收尾**：`dotnet build` → `npm run build` → 更新 `wwwroot/index.html` 的 `?v=`（兩處）
→ 更新 `CLAUDE.md` / `FIELD_SPEC.md` / `DB_table.md` / `memory.md` / `docs/使用者手冊.html`。
測試若動到真實資料，**做完一定要還原並複驗**。

---

## 3. 使用者否決過的做法（不要再提，也不要「順手做回去」）

| 被否決的 | 使用者的話 / 理由 | 日期 |
|---|---|---|
| 精簡模式加上編輯／刪除入口（含「把 ✎ 放進展開明細」「讓窄螢幕能關掉精簡模式」） | 「**主管瀏覽時使用精簡模式，不需要看到編輯或刪除的功能**」—— 那是**唯讀的主管檢視**，不是欄位少一點的編輯畫面 | 2026-09-05 |
| 用登入身分篩「只看我的需求」 | 「**我沒有用全名，用篩選無效**」＋「有時候登入的人可能是主管，不需要」。⚠️ 不要假設「工號 → 姓名」推得出來的名字等於控表負責人欄裡的字串 | 2026-09-05 |
| 「未結案／已結案／全部」分段控制 | 「**我上述五個按鈕的功能也要保留，會不會畫面太混亂?**」—— 與 StatusID 那排是同一群資料的兩組字，而且兩顆會互相打架（點 `5 結案`、分段控制停在未結案 → 0 筆又看不出原因）。**那五顆是主要導覽，要減的是「列」不是「控制項」** | 2026-09-05 |
| 放不下就自動收欄（`tight`）／放大前先問要不要切精簡模式 | 「**這方式不行，非精簡模式下若我想要看到全貌、放大看還是會破圖**」「**我也不能夠強迫其他人若放大只能看精簡模式的資料**」。放大是為了看清楚，不是為了少看七欄 | 2026-09-05 |
| 「整個畫面等比例放大、框架也一起放大」 | 橫捲的條件是「表格需要的寬度 × 倍率 > 螢幕寬」，把倍率調大是**題目本身不是解法**。投影模式就是那個做法的完整實作，它連續破版兩次，第 32 批的結論反而是「必須先切精簡模式」 | 2026-09-05 |
| ~~「已略過此階段」的**事後補記完成**按鈕~~ | 2026-09-10 他選「先不動，我再想想」；**2026-09-12 第 70 批第三輪選了做**（「補記完成…」，見第 7 節）。留這一列是因為他中間改過主意 —— 日後別再拿「他 9/10 說先不動」當依據 | 2026-09-10 → 09-12 |
| 匯入套 H4/H5、匯入倒序 `min>max` 死結 | 「**4 不用做**」（2026-09-12）。匯入來的洞改由「補記完成…」補；`min>max` 時補記鈕不出現、tooltip 講要先修哪兩個日期。⚠️ 我問過「可以移除」是不是指整個拿掉匯入功能，他說**不是** —— 匯入功能留著 | 2026-09-12 |
| **移除投影模式（📽）** | 2026-09-22 他自己問「目前的網頁，還需要上述這顆投影按鈕嗎?」（附反灰的截圖），我把「留 vs 拿掉」量完給他挑 → **「還在用投影機 → 保留現狀」**。⚠️ 決定關鍵是**真的打在布幕上 vs Teams 螢幕分享**，不是程式碼成本 —— 它那 5 件事（200% 放大／加深色階／斑馬紋／淺色底／收起含匯入的整組寫入鈕）只有投影機情境才成立，而他們還在用。⚠️ 連帶：`clipPx`／`renderClipWarning`／`scrolledX` 雖然「只為投影而存在」（第 46 批第四段），**一樣不要清掉** | 2026-09-22 |
| 表格外包 `overflow-x:auto` | 會廢掉兩層 sticky 表頭與左側凍結欄整套設計 | — |
| 改用 `transform: scale()` 放大 | `input.css:257` 已寫明踩過 | — |
| 通知信「副本與收件者同一人時去重」 | 「**EMS 跟 MSD 人員不會是同一個，除非是我要測試用，那我還是需要看到寄件者跟附件收件者**」 | 2026-09-03 |
| 限制手動能寄幾封信 | 「**手動寄信不用限制使用者要寄幾封，但寄信需要留下歷史紀錄**」 | 2026-09-03 |
| 第二套版面／第三個頁籤（卡片版 RWD、到期預警頁、主管專用表） | 第 12 批已因「不再維護第二套格式」拿掉過到期預警頁。主管檢視一律用**隱藏／重排既有欄位**達成 | 2026-08-19 |
| 把 `Status` 併進 `StatusID` 省一欄 | **2026-08-22 使用者要求復原，不要再自作主張併回去** | 2026-08-22 |
| 「延期完成 2 次以上」篩選選項 | 第 38 批移除、併回「有延期完成」。⚠️ 徽章的「2 次以上轉紅」**刻意保留** | 2026-08-27 |
| 2026-09-05 UI/UX 清單的 **F**（頁首顯示姓名）／**G**（誤刪的復原出口）／**H**（批次通知） | 「**F、G、H 不需要修改**」（沒有給理由，不需要追問）。⚠️ 這三項**不是「還沒排到」，是已經被否決** —— 下一次的 UI/UX 建議不要再列一遍 | 2026-09-05 |

---

## 4. 還沒做的事

### 需要使用者／DBA 動手的
- [ ] **瀏覽權限上 IIS 的三步**（第 74/75 批，2026-09-21）：①在正式 DB 跑 `18_add_access_control.sql` 與 `19_add_access_admins.sql`（`19` 會種入 `00002732`／`00041817`／`yu-tinglin`，**正式 DB 建議先把 `yu-tinglin` 那一列拿掉**）；
      ②`appsettings.json` 的 `Access:Admins` 維持空清單 —— 管理者已經在 DB 裡，之後在面板的「管理者」區塊加人；只有 DB 那份被刪光時才臨時填一個工號進設定檔救回；
      ③確認 `[WEB].[dbo].[notes_person]` 從 Controltable 用的連線帳號查得到（Gantt 同一台 DB 已經在用，通常沒問題）。
      然後進網頁 → 🔐 → 先加規則（例：`DEPT_3 = MSD`）→ 用「工號測試」拿幾個工號驗過 → 再開卡控。**開關預設 false，部署當下不會鎖任何人。**
- [ ] **`16_grant_dbmail_permission.sql` 尚未執行**（要 DBA 在 DB 主機以 sysadmin 執行）。
      ⚠️ 腳本裡 `@LoginName` 預設 `testuser`，**正式環境要先改**。已用 `SET PARSEONLY ON` 驗過語法。
      只有切到 `Mail:Mode = "dbmail"` 才需要它 —— 目前生效的是 `smtp`，**不執行也不影響現況**。
- [ ] **`.gitignore` 已建立（第 23 批），但既有的追蹤還沒解除** ——
      `node_modules/`（3332 個檔案）與 `bin/`＋`obj/`＋`.vs/`（117 個）**都還在版控裡**。
      `.gitignore` 只對未追蹤的檔案生效，要真的清掉必須執行一次：
      ```
      git rm -r --cached node_modules bin obj .vs
      git commit -m "套用 .gitignore：停止追蹤建置產物與 node_modules"
      ```
      **刻意沒有代為執行** —— 那會一次暫存 3449 個檔案的刪除，是使用者才能決定的動作。
      ⚠️ `wwwroot/app.js` 與 `app.css` **刻意保持追蹤**（沒有 CI，部署就是複製資料夾）。
- [ ] 確認新結構無誤後，可自行 `DROP TABLE dbo.Controltable_bak_20260816`（2026-08-16 遷移前的備份）。

### 要先問過使用者才能做的
- ✅ 第 68 批的第 5、6 項、第 70 批列出的全部（撤銷視窗、⏰ 文案、`DonePastHint`、事後補記）都已於 2026-09-12 做掉；
      匯入 H4/H5 他決定**不做**（見第 3 節）。**這一輪的規則檢查清單已經清空。**
- [ ] **C8：把 EMS／MSD／逾期／進度／警示五個下拉收進一顆「篩選 (N)」面板** ——
      會犧牲可發現性，且動到已寫進 `FIELD_SPEC.md` 的工具列規格。
- [ ] **A10：新增視窗沒有註冊日期欄**（`openAdd` 有帶今天當預設，只是不能改）。

### 長期想法（尚未與使用者確認優先序，依投報率排序）
1. **迷你甘特條**：三個時程欄改成視覺化長條 + 今日紅線，主管掃一眼就知道卡在哪
2. **Spec 基線**：MSD 按下 Confirm 時快照 Spec 欄位，之後任何異動標紅「Spec 已於 Confirm 後變更」——**主管最想抓的情況**
3. **稽核表擴充到非日期欄位**：目前只記四個階段的日期異動，`MainCat`／`SubCat`／`Remark`／現況說明被改追不到
4. **主管檢視 tab**：本月新增數、平均 Spec→上線天數、延期 TOP 5、Confirm 後仍被異動的清單
5. 分頁或虛擬捲動（**64 筆還不需要**，而且使用者 2026-08-19 要求全部攤開）
6. **Next check 藍色標示**（`FIELD_SPEC.md` 唯一還掛著「未實作」的項目）

> ✅ 2026-08-22 起那份 UI/UX 體檢清單（P0 → A~G）已全部做完。
> **下一輪要再體檢請重新分析，不要照抄舊表。**

---

## 5. 這台機器與驗證方法的坑

- ⚠️ **`python` 是 Windows Store 的假殼，一律 exit 49** —— 改用 **node** 或 PowerShell 的
  `Invoke-RestMethod` / `ConvertFrom-Json`。
- ⚠️ **截圖拿不到**（Browser 面板未顯示時不合成畫面，一律 5 秒 timeout）。
  **驗證一律用 DOM 量測**：`getComputedStyle` / `getBoundingClientRect` / CSSOM。
- ⚠️ **分頁不在前景時 `scroll` 與 `resize` 事件根本不會送出**（實測連自己掛的 listener 都是 0 次，
  而 `scrollX` 確實變了）。要驗得用 `dispatchEvent(new Event('resize'))`。
  **同一類**：背景分頁的 `requestAnimationFrame` 不會被呼叫（量 overflow 一律同步量，不可包 rAF），
  **動畫與 transition 也不會跑**（量到「沒有變化」不代表樣式壞了）。
- ⚠️⚠️ **transition 沒跑完時，`getComputedStyle` 回的是「動畫中途的值」，不是最終值**（2026-09-07 又踩一次，
  差點誤判成一個不存在的 bug）。`.t-card` 有 `transition: background 0.3s`，而深色模式是掛載後才套上去的 ——
  在隱藏的 Browser 面板裡那個 transition **永遠停在起點**，於是量到「`--bg-card` 是 `#1e293b` 但
  `backgroundColor` 是白的」這種自相矛盾的結果。**判斷方式**：新建一個同 class 的元素來量（它沒有進行中的
  transition），或量之前先 `el.style.transition = 'none'`。**兩個值對不起來時先懷疑 transition，不要先懷疑 CSS。**
- ⚠️ `resize_window` 的 `preset:"desktop"` 之後 `innerWidth` 可能量到 **0**；
  Browser 面板卡死（JS eval 30s timeout）的根因也多半是**視窗尺寸變成 0x0**，重設尺寸即可，不要急著關分頁重開。
- ⚠️ **不要信既有節點的 computed 值去判斷深淺色**：讀既有 `<tr>` 的 `borderBottomColor` 會一直回傳深色值。
  量深淺色一律拿**新建元素**或 CSSOM 規則來對。同理讀按鈕的 `backgroundColor` 判斷「哪一顆是選中的」也出過假象。
- ⚠️ **`preview_start` 跑的是 `dotnet run --no-build`**：改了 C# 要先 `preview_stop` 舊的 → `dotnet build` → 再 `preview_start`。
  舊 process 開著時 build 會「dll 更新了、exe 複製失敗」而且 `--no-build` 照樣起得來 —— 測到的是舊程式（第 68 批踩過）。
- ⚠️ **中文驗證不看 sqlcmd 的畫面輸出** —— 這台的 console 是 Big5，一定是亂碼；
  要驗中文改用 API 回傳的 JSON 或 `-o` 導檔（UTF-8）。
- ⚠️ **`Get-Content` / `Set-Content` 會把含中文的檔案燒成亂碼且不報錯** —— 改檔一律用工具或 `sed -i`。
- ⚠️ **完整寄信在本機測不了**：`Sariel` 是 Developer Edition 但沒啟用 Database Mail；
  也試過在本機起 PowerShell 的 SMTP sink，**連不上**（沙箱裡 `TcpListener` bind 得起來但外面連不進去）。
  `Mail:Host` 空的時候是用「落地成 .eml 檔」的暫時 patch 驗的。
- ⚠️ **p58esiap08 能寄不代表 p58esiap12 能寄** —— 內網 relay 多半依**來源 IP 白名單**放行。
- ⚠️ **QUOTED_IDENTIFIER 的坑**：篩選索引 `IX_Controltable_Active` 會要求對本表的任何 DML 都在
  `SET QUOTED_IDENTIFIER ON` 下執行，而 sqlcmd 預設是 OFF。第一次跑 `07` 時兩段 UPDATE 整批
  `Msg 1934` 失敗，但腳本「跑完了」，只有 PRINT 的「回填 0 筆」透露出異常。
  **日後含 DML 的累加腳本，每個批次都要自己帶這行。**
- ⚠️ `TRY_CONVERT(DATE, '')` 回傳 **`1900-01-01` 而不是 NULL** —— `01` 腳本因此把 12 個空日期寫成 1900 年，
  而檢查語句剛好排除空字串所以誤報 0 筆失敗（由 `03` 修正）。**寫檢查語句時不要把要抓的情況排除掉。**
- ⚠️ **SQL 腳本自己印的「修正 N 筆」不一定可信** —— `14` 腳本第一次執行時 `FixedRows` 印出 0，
  但那一列其實已經改成功了。**改完一律另外下一句 `SELECT` 去查實際結果**，不要只看腳本的 PRINT。
- ⚠️ 匯入用 ClosedXML 時**不可用 `RowsUsed()`**（格式化但內容空的儲存格判定不一致，會漏列），
  一律 `LastRowUsed().RowNumber()` 逐列迭代。2026-08-18 漏掉 2 筆就是這個。

---

## 6. 版面預算（動之前先量）

| 量的東西 | 值 | 出處 |
|---|---|---|
| 16 欄表格 min-content | **1256px**（整頁需求 1281px） | 第 46 批實測 |
| ↑ 舊紀錄 1237 / 1254px | **已過期，改讀 1256** | 第 31 批 → 第 39 批 → 第 46 批 |
| 9 欄（精簡模式） | **901px** | 第 31 批 |
| 工具列（1217px）算式 | `搜尋 220 + 漏斗 34 + 分隔 1 + 140×5 + 動作 168 + gap 8×8 = 1187`，**只剩 30px** | 第 36 批 |
| 階段那一排（1440 螢幕、可用 1425px） | 左 `ALL`+五顆 **725px**、右 `需關注 170 + 顯示 N/M 81 + 精簡＋排序 168`；目前 need **1224px** | 第 51 批 |
| 第一列資料的起點 | **489 → 363（第 50 批）→ 321px（第 51 批）**；一列高 79px | 第 50/51 批 |
| 「⚠ 未壓日期」徽章 | **61.99 × 21px**（`<span>` 與 `<button>` 完全相同）；精簡模式那一欄寬 **96px** | 第 54 批 |

⚠️ **這些數字全部會過期，`min-content` 自己會算 —— 不可以寫死進程式**（第 46 批那條 `.page-shell` 的教訓）。
⚠️ **加東西之前一定要在 1366／1440 兩個寬度量**：第 51 批就是只量了 1280 與 1600，漏掉 1440 而破版被回報。

---

## 7. 批次紀錄（新 → 舊，只留結論與坑）

### 第 80 批（2026-09-22）右欄在 1366／1440 工作機上看得見
- 起因：使用者問「可以參考 C:\Gantt 的版型、在版面右邊新增小節嗎? 比較符合 UX」——**那個功能第 79 批就做好了**，但斷點 1500px ＋ 直接 `display:none`，而 1366／1440 筆電是主要工作機 → **他從來沒看見過**。⚠️⚠️ **看不到 ＝ 沒有做**（同第 57 批「刻意的限制沒講出來，在使用者眼裡就等於壞掉」）。順帶一提：Gantt 的手冊其實**沒有**右欄（只有左側側欄含子目錄），這一份是它沒有的。
- 做了什麼：中間加一層 `@media (max-width:1500px) and (min-width:1281px)` —— **只加左右 28px 留白 ＋ gap 32，欄寬交給 grid 自己算**；1280 以下整個收掉回兩欄。
- ⚠️⚠️ **第一版寫死 700px 是錯的，而且是使用者看了截圖問「版面顯示比例建議在調整嗎?」才抓到的**。我當時拿第 79 批「拉寬沒有用」去推「縮窄也沒有代價」—— **推錯了**：那筆量的是 900→1300（`p` 早被 `70ch` 封頂、表格也早就不換行），700→900 完全是另一回事。實測 1400 寬、內容欄 700 vs 893：表格總高 **13705 → 11697（−15%）**、最高那張 **1110 → 906**、第 17 章 **4311 → 3528（−18%）**、全文 56649 → 54694。**段落沒變，被壓高的是表格。**
- ⚠️ 也不必自己算寬度：**`minmax(0,900px)` 的軌道本來就只長到「剩多少」為止**（fixed max 的軌道不會超過可用空間）。實測 1400 → 837、1366 → 803、1281 → 718，全部是 grid 自己算的。**寫死的數字會過期，`minmax` 不會**（第 46 批 `.page-shell` 同一條）。
- ⚠️⚠️ 下界從 1240 提到 **1280**，界線是「**右欄不可以從讀的那一欄身上偷寬度**」：文字區 1440 → 821、1366 → 747、1280 → **661**（仍高於 `p{max-width:70ch}` ＝ 633px）、1241 → **622 已破線**。破線之後等於犧牲正文換一份導覽。1280~1366 留著的代價很便宜：全文總高 +0.5%（1366）／+2.4%（1280）。
- 實測七個寬度（1600／1440／1366／1281／1280／1024／820）：右欄 ✓✓✓✓✗✗✗、內容欄 900／877／803／718／900／759／805、**橫捲量全部 0、沒有任何 table／section／svg 溢出**。列印規則不受影響。只動 CSS，**不需要 `npm run build`**，版本仍 `20260922003`。

### 第 79 批（2026-09-22）手冊改三欄：右側「本章小節」
- 起因：使用者看 1920 的截圖問「右半部是不是有一個區塊都不會使用到，是否有點浪費空間?」。**先量再答**：內容右緣固定在 1150px（側欄 250＋內容 900），1920 螢幕右邊空 **770px＝40%**、2560 空 1410px。
- ⚠️⚠️ **但「把內容拉寬去填滿」是錯的解法，而且是實測出來的**：①段落真正的上限是 `p{max-width:70ch}`＝**633px**，把內容欄拉到 1300 仍然是 633；②表格的列高被 `th` 固定欄寬（250/210px）決定 —— 中間欄 334→**734px**（多一倍多）而最高那一列**還是 112px，一個 px 都沒省**；③33 個表格沒有一個需要橫捲。**空間要用就是放東西，不是把行寬拉長。** 我把三個選項（置中／右欄導覽／維持現狀）連同這些數字給他挑，他選右欄導覽。
- 做了什麼：`.colwrap` 包住 `main` ＋ 新的 `<aside class="onthis">`，兩欄**合起來置中**；左側側欄的子目錄**整批搬到右欄**（`.rail a.sub` 那套 CSS 與 JS 全部移除），右欄只列**目前這一章**的小節並標出正在讀的那一節（第二個 IO 看 `h3`）。
- ⚠️⚠️ **`pickActive()` 是重點，不是可有可無的備援**：原本純靠 IntersectionObserver，結果右欄一直是空的 —— 查出 `document.hidden` 為真時 **IO 完全不派送**（與「背景分頁 rAF 不會被呼叫」是同一類坑，這專案第 30 批踩過）。而且就算在前景，**IO 第一次回呼之前右欄也是空的**。現在載入／`hashchange`／`applyRole` 之後各直接算一次。⚠️ 第一版 `pickActive()` 在**最頂端時挑不到任何章節就 return**，右欄在剛載入時仍是空的（抬頭＋身分列正好佔滿 10%~30% 那一帶）—— 一定要退回第一章。
- ⚠️ 斷點 **1500px**（不是 1400）：1440 時 `250+900+40+210` 只剩 25px 餘裕、側欄與內容貼在一起，而 1366／1440 筆電是主要工作機 → 那裡回到兩欄置中。
- 實測：1366 兩欄（右欄 `display:none`）／1440 兩欄 左138 右153／1600 三欄 左93 右108／1920 三欄 左253 右268 右欄 5 項／2560 左573 右588，**五個寬度都沒有橫捲**；載入→A 章 5 節、跳 `#c5`→4 節、跳 `#c10`→4 節；MSD 身分下跳 `#rb`（EMS 專屬）自動切回「全部」且右欄列出 B 章 6 節；切身分後右欄**沒有任何連結指向被隱藏的章節**；左側側欄的 `a.sub` 歸零；列印三條規則（`.colwrap` block／`.onthis` none／`section.hidden` block）都在；無斷鏈、無 console 錯誤。版本 `20260922003`。

### 第 78 批（2026-09-22）手冊增補：被擋訊息對照表 ＋ 資料列線框圖 ＋ 完整範例
- 起因：使用者「有建議可以更讓使用者了解操作的設計方式嗎?」。我**先量再建議**：後端有 **112 種 400／409 擋下訊息**、前端 8 個就地標紅 ＋ 15 個彈窗群組，而手冊的疑難排解**只有 12 條 FAQ** —— 使用者被擋的那一刻最需要手冊，卻有一百多種訊息查不到。提了六項排序後他選 1、2、3（速查卡沒選），圖維持線框圖不改截圖。
- 做了什麼：①**第 17 章「被擋下來時看到的訊息」**（33 列，五節 `m-save`／`m-done`／`m-rollback`／`m-mail`／`m-misc`，格式一律「看到這句話 → 為什麼 → 怎麼辦」），第 15 章加指路；②**資料列線框圖**＋9 個編號標註（風險條、⏰/🔄、StatusID 藥丸、⚠N、延期完成兩行、⚠ 未壓日期、✉、逾期紅字、結案列看不到）——A 章現在兩張圖（資料列＋編輯視窗）；③**「例」章（`#rex`）**：NID 99 從建立走到結案 8 步，每步寫「誰在做／按哪裡／畫面變成什麼」，含「開發晚三天的兩種記法」對照表；④`alertModal` 右上角加 `?`。
- ⚠️⚠️ **`manualAnchorFor()` 依標題推小節，踩到並修掉一個真的 bug**：「必填欄位**未完成**」被 `/完成/` 命中 → 連到「標記完成」那一節（實測 `#m-done`）。**順序有意義、而且要先排除**（`未完成|尚未儲存|未儲存` 排最前）。修完**拿程式裡實際存在的 36 個標題全部跑過一遍**才算數（scratchpad `anchor.js`，最後一個 `這筆資料已被其他人修改` 也補進規則）。⚠️ 依**標題**不依訊息內容：訊息是後端自由文字，文案一調就靜靜失準。⚠️ 推不出來退回整章 `c17` —— **錯的錨點比沒有錨點更糟**。
- ⚠️ 為了不動 `#c1`~`#c16` 錨點（`?` 鈕與交叉引用都指著它們），新章節取 `c17` 放在最後、範例章取 `rex` 插在 A 與 B 之間（`data-num="例"`）。實測全文內部連結只有 4 條，沒有一條被打斷。
- 實測（1440）：22 章、33 個表格、2 張線框圖、第 17 章 33 列；全部 54,757px／EMS 46,984／MSD 44,135／主管 44,241；`m-*` 五個錨點全在、無斷鏈、SVG 沒有元素超出 viewBox、無 console 錯誤、無橫捲；`alertModal` 的 ? 實測導到 `#m-save`，彈窗 448px 寬、標題 344px 沒被擠到換行。版本 `20260922002`。

### 第 77 批（2026-09-22）手冊改版：參照 `C:Gantt` 的版型 ＋ 視窗裡的 `?`
- 起因：使用者「可以參照 C:Gantt 的使用者手冊的方式修正嗎? 目前的版型好像不太適合閱讀」。看過 `C:Gantt\wwwroot\使用者手冊.html` 後，它贏的**不是配色是三個結構**：①頂端身分篩選（藏掉不屬於你的整章）②`details` 收合邊界案例③左側深色側欄＋JS 自動生成目錄＋scrollspy。另外它有「系統各視窗的 `?` 直接開到對應章節」。他選「全部做」＋「? 鈕先做幾個重點視窗」。
- ⚠️ **量完才決定收哪裡**（不是憑感覺）：全文 45,138px，**49 個 `.note` 共 12,022px ＝ 27%**，單一最大是「寄不出去的時候」**977px**；最長兩章是 05 標記完成 5,131px、10 逾期與需關注 5,099px。
- 做了什麼：側欄 250px（深色 `--brand-deep`）＋內容欄 **900px**＋字級 **15→16px**；目錄改成 JS 依 `data-group`/`data-num`/`data-title` 生成（**舊的手寫 `<ol>` 已刪**）；身分篩選 EMS／MSD／主管／全部；長章節的 h3 子目錄**只在目前那一章展開**（20 章全列會是 64 條）；7 塊 rationale 收進 `details.adv`；`h2` 右邊加身分小標；一張「編輯視窗」線框圖＋6 個編號標註；深色切換鈕。`app.jsx` 加 `ManualLink` 掛六個視窗。
- ⚠️⚠️ **兩條界線是這一批的重點，不要往回退**：①**EMS 與 MSD 共用大半章節**，所以身分篩選只藏真正不相關的（藏錯 ＝ 使用者以為沒這功能，比文件長嚴重）；②`details` 只收「為什麼這樣設計／罕見失敗」，**正常會遇到的行為一律留主線**。列印時兩者都要攤開（`section.hidden{display:block!important}`）—— 拿紙本的人沒有開關可按。
- ⚠️ `?` 鈕**不帶 `?role=`**（系統不知道登入者是 EMS 還 MSD），只帶 `?theme=` 且不寫進 localStorage。固定錨點 `#h-undo`／`#h-timeline` 寫在 `<h3 id>` 上，**不可用 JS 自動產生的 `c5-s4`**（依序號，插一個 h3 就歪掉）。
- ⚠️ 踩到的坑：①**本機 `file://` 開手冊 JS 不會跑**（預覽是靜態快照），一律用 `/manual` 跑；②`new RegExp(字串)` 版的 `\\s` 在這裡配不到而字面量可以 —— 批次改檔改用 `indexOf` 掃描，不要跟正規表達式的跳脫纏鬥；③一個 `.lb` 內含 `<b>`，`[^<]*` 截不乾淨，手動補修。
- 實測（1440）：全部 44,952px、**EMS 37,177（−21%）、MSD 34,327（−27%）、主管 34,434（−27%）**，收合再省 2,373px；目錄 20 條＋子目錄 64 條、群組 2 個、scrollspy 正確；`msd` 身分開 `#c3`（EMS 專屬）→ **自動切回「全部」並捲到位**（c3Top 128）；`?theme=dark` 生效且 `ct_manual_theme` 仍是 null；六個錨點全部存在；列印三條規則都在；App 頁首仍 65px 單行、無橫捲、無 console 錯誤。版本 `20260922001`。

### 第 76 批（續，2026-09-21）手冊掛上網頁：`GET /manual` ＋ 頁首 `📖 手冊`
- 起因：使用者「幫我把手冊路徑加到網頁上，讓使用者可以點選瀏覽」。
- 做法：**沒有把檔案搬進 `wwwroot/`**（CLAUDE.md 原本寫的是那條路）—— 改用 `app.MapGet("/manual")` 直接讀 `ContentRootPath/docs/使用者手冊.html` 回傳。理由是**單一來源**：複製一份進 wwwroot，日後一定只會改到其中一邊。網址取 ASCII（中文檔名會變一長串 `%E4%…`）。前端頁首在「字級」與「深淺色」之間插一顆 `<a href={api('/manual')} target="_blank" rel="noopener" className="ctl-sm">📖 手冊</a>`，`!present` 時才出現。
- ⚠️⚠️ `Controltable.csproj` 加了 `<Content Include="docs\使用者手冊.html" CopyToPublishDirectory="PreserveNewest" />` —— **少了它開發機完全正常、IIS 上卻 404**（`dotnet publish` 不會帶 `wwwroot` 以外的檔案；開發機的 ContentRoot 就是原始碼目錄所以測不出來）。這是這一批唯一一個「本機驗不到」的風險點。
- 實測（另開 5299 埠跑 scratchpad 的 build，沒碰使用者自己的 5146/7127）：`/manual` → **200 · text/html; charset=utf-8 · 140,901 bytes · `Cache-Control: no-cache`**，標題與「依角色操作」都在；`publish` 輸出目錄底下確認有 `docs/使用者手冊.html`。頁首鈕 `href="/manual"`、`target="_blank"`、寬 **61.7px**，**1280／1366／1440 三個寬度頁首都仍是 65px 單行**（scrollW 1265／1351／1425，沒有橫捲）。版本號兩處帶到 `20260921003`，手冊抬頭同步。
- 手冊自己也更新了（第 02 章頁首表多一列、A 章「我想要…」多一列「再打開這份手冊」）。
- ⚠️ 使用者自己開著的 `Controltable.exe`（PID 12516）跑的是舊的 DLL，**要重啟才會有 `/manual`**。

### 第 76 批（2026-09-21）使用者手冊改成「第一部依角色、第二部功能參考」
- 起因：使用者說「可以依照 EMS、MSD 登入者的角度去指導它們怎麼操作嗎？目前的設計好像很難懂」。舊手冊是 16 章的**功能參考**（依畫面與功能排），一個 EMS 打開找不到「一筆需求落到我頭上，我要按哪裡」這條路。
- 做了什麼：**只有 `docs/使用者手冊.html`**，沒動程式。在原 16 章前面插入第一部（A 先看這一頁／B EMS 負責人／C MSD 負責人／D 主管），原 16 章成為第二部、**編號與 `#c1`~`#c16` 錨點一個都沒改**（全部交叉引用仍有效，實測 `brokenAnchors: []`）。A 章的核心是「每個階段只有兩個動作：壓結束日、標記完成…」＋「我想要…」對照表；B／C 是從頭到尾的手順（`ol.steps`），每步附「詳見第 N 章」。
- ⚠️ 手冊裡兩句**刻意照實寫**的行為，日後改程式時要回來對：①「**標記完成之後系統不會自動問要不要寄信**」（`askNotifyUnset` 只掛在 `handleSave`，`/done` 那條路沒有）—— 那正是最自然的交棒時刻，已向使用者提出但未做；②「EMS 自己建需求、① 沒壓日期時，存完會問要不要通知**自己**」（`UnsetPhaseOf` ① 的收件者是 EMS）—— 手冊教他按取消。
- 沒動 `index.html` 版本號（只改手冊），抬頭仍對齊 `20260921002`。Artifact 那份（`751c4197…`）**沒有重新發佈**，等使用者說。

### 第 75 批（2026-09-21）管理者清單從 appsettings 搬進 DB
- 起因：使用者在 `appsettings.json` 填了 `["00002732","yu-tinglin"]`，面板測 `00002732` 卻「會被擋下」。查 `/api/access-rules` 回的 `admins` 只有 `["yu-tinglin"]` —— **.NET 設定檔的 JSON 陣列跨檔是依索引覆寫不是合併**，我放在 `appsettings.Development.json` 的 `["yu-tinglin"]` 蓋掉了他的第 0 筆。兩個檔看起來都沒錯，這種坑不該留給業務資料。使用者問「改成寫在 DB 端是否會比較好」→ 我建議搬、他說做。
- 做了什麼：`19_add_access_admins.sql`（`dbo.AccessAdmins`、種入三個工號、`CK_AccessLog_Action` 多 `ADD_ADMIN`/`DELETE_ADMIN`）；`Program.cs` 的 `AccessAdminsAsync(conn)` ＝ DB ∪ `ConfigAdmins()`、`IsAccessAdminAsync()` 給寫入端點當門、`POST/DELETE /api/access-admins`；面板多「👑 管理者」區塊（DB 那份逐筆可刪、設定檔那份虛線框標「設定檔後備」、自己那列 disabled）。`appsettings.json` 的 `Admins` 改回空清單、Development.json 整段拿掉只留說明。
- ⚠️ **設定檔那份刻意保留當後備**，不是忘了刪：第一個管理者從哪來、DB 那份被刪光了怎麼辦，都靠它。「不能刪自己」「不能刪最後一位（含設定檔的）」兩道後端都擋（實測 400：「不能移除自己。請由另一位管理者操作」）。
- ⚠️ `ConfigAdmins()` 另接受字串 `"a, b"` —— 字串是整個值覆寫，跨檔不會合出奇怪的結果。
- 實測（本機）：`00002732` 測試 → 可以瀏覽·管理者；新增 `UMC\00099999` → 剝網域存成 `00099999`、重複 409；刪自己 400；刪新加的 200；`AccessLog` 多 `ADD_ADMIN`/`DELETE_ADMIN` 兩筆；`19` 重跑種入 0 筆。版本 `20260921002`。

### 第 74 批（2026-09-21）頁面瀏覽權限卡控（對齊 `C:Gantt`，四個差異點由使用者拍板）
- 使用者原話：「限制特定人員才能瀏覽此網頁」「做法會跟 C:Gantt 專案的相同、一樣是透過 `[WEB].[dbo].[notes_person]` 資料表來控管」「請先參考 C:Gantt 專案…確認後再跟我說一聲」。
  先看完 Gantt 的 `11`/`12` 腳本、`Program.cs:1602-1786`、`app.jsx` 的 `AccessPanel`／`AccessDeniedScreen`／載入閘門，整理成一份對照後**用四個選項問過再動手**，他四個都選了建議項。
- 做了什麼：`18_add_access_control.sql`（`AccessRules` / `AppSettings` / `AccessLog`）＋ `Program.cs` 六支端點（`access-status` 匿名、其餘 Negotiate；規則與開關的寫入包交易、寫 `AccessLog`）＋ `app.jsx` 模組層 `checkAccess()` / `AccessGateScreen` / `AccessDeniedScreen` / `AccessPanel` ＋ App 的閘門與頁首 🔐。
- ⚠️ **與 Gantt 的三個刻意差異**（`CLAUDE.md` 有完整版）：工號後端自己讀、管理者走 `Access:Admins`（一律可瀏覽）、沒有 SP。**只擋畫面**這一點與 Gantt 相同。
- ⚠️ **通過檢查之後才 `fetchReqs()`**：原本 `useEffect(() => { fetchReqs(); … }, [])` 一載入就抓。被擋的人畫面上沒有列，但 65 筆早就在他的 Network 面板裡 —— 改成 `accessPassed` 為 true 才起第一次抓取（`dataStartedRef` 保證只起一次）。實測被擋時 `performance.getEntries()` 裡只有 `/api/access-check`。
- ⚠️ **401 與錯誤要分開**：`/api/access-check` 掛 Negotiate，非網域瀏覽器拿到 401 → 不是錯誤、是「沒有工號」→ 改問匿名的 `/api/access-status` 決定放不放行。其餘非 2xx 與 15 秒逾時一律錯誤畫面＋重試、**不放行**（Gantt 註解裡那段「fail-open 等於把閘門做成裝飾」照搬）。
- ⚠️ **`Access:Admins` 即時讀取**（`app.Configuration` 每次呼叫時 `Get<string[]>()`），實測改 `appsettings.Development.json` 不重啟 2 秒內 `isAdmin` 翻過來。加一個管理者不必重啟 IIS。
- ⚠️ **本機模擬名冊裡有 `EMPNO = 'yu-tinglin'`**（Gantt 的 sim 腳本放進去的），所以這台機器的帳號在 `DEPT_3=MSD` 規則下會**過**，測「不在名冊」要用 `?testEmpId=` 拿別的工號（例：`00040704` IMD → 擋、`00034018` EMS2 → 擋）。
- 實測（本機，全部還原）：加規則 `DEPT_3=MSD（MSD 全員）` → 開卡控 → 重整仍可看（管理者）→ 清空 `Admins` → 重整仍可看（規則過，🔐 消失、四支管理端點全 403）→ 把規則改成 IMD → 重整 → **整頁 🚫 無權限**（工號／名冊姓名部門／原因三段都印出來，沒有頁首、沒有資料）→ 還原 Admins → 刪規則、關卡控。`AccessLog` 留下 4 筆。頁首 1280 寬仍 65px 單行、🔐 37px。
- 版本 `20260921001`。使用者手冊新增「瀏覽權限」一節。

### 第 73 批（2026-09-13）全面體檢（rule ＋ 版面）→ 七項全做
- 使用者：「幫我檢查一下，目前的rule跟版面還有哪些需要修正或建議優化的項目」→ 列了 L1～L3／R1～R4，他說「都做掉」。
  **規則面沒有找到會寫壞資料的 bug**（六對前後端鏡像、樂觀鎖對空 token、四條寫入路徑都更新 UpdatedAt、62 筆不變量掃描都對過；
  新增的資料狀態只有 **NID 6**：① 01-06 晚於 ② 01-05、StatusID 3、兩階都走完，匯入來的、不影響操作）。
- **L1** 1280 螢幕階段那一排斷成兩行（102px）：`需關注` 後面的「· 未壓 N · 逾期 M」寬度隨資料變（各 38px），第 51／63 批的預算是拿當天件數量的。
  拿掉兩段、細項進 tooltip → 170 → 82px，1280 兩種模式都回 60px（規則進 CLAUDE.md）。⚠️ 這是資料一變就回歸的那種，不是程式碼改壞的。
- **L2** 編輯視窗四個階段底下的「異動紀錄」由逐筆＋110px 內嵌捲軸改成與明細列同一份 `PhaseChainRow`（第 72 批那段從明細列**抽出來**共用，
  明細列的輸出逐字相同、313px 不變）；右邊「完整軌跡 ↗」開 `histModal` 篩到該階段（`openHistFor`）。實測 NID 62 的 ① 108px 可視／160px 內容 → 93px 無捲軸。
- **L3** `@media print` 加 `[data-ct-modal]{display:none}`。**R1** 匯入確認視窗列出會歸零的四樣（軌跡／實際完成日／計數／通知）。
  **R2** 投影鈕 tooltip 拿掉「頁首與工具列會跟著橫向捲走」（第 46 批後已不成立）。**R3** 「減回 1」→「減 1」（後端 note ×2、前端 ×3、手冊）。
  **R4** `removeItem('ct.duePriority')` 搬進 `useEffect(…, [])`。
- ⚠️ **Program.cs 的改動（只有 R3 那兩句 note）編譯過了、但 exe 被使用者自己開的 process（PID 19920，https 設定檔，5146＋7127）鎖住沒複製** ——
  刻意沒有 kill 他的 process。**他下次重啟 `dotnet run` 才會生效**；前端是直接從 wwwroot 讀檔，已經是新的（`?v=20260913005` 實測）。
- ⚠️ Browser 面板這次又壞過一次（reload 後整頁空白、navigate 被拒），開新分頁（`tabs_create`）就好，不必重開伺服器。
- 沒做、等他決定：C8、A10、`.gitignore` 清 tracked 檔、`Controltable_bak_20260816`；第 66～73 批的檔案都還沒 commit。


### 第 72 批（2026-09-13）明細列的「時程變更軌跡」改成依階段收合 ＋「完整軌跡」視窗
- 使用者附截圖：「時程變更軌跡，可以提供更簡潔的呈現方式嗎? 不然變更一個步驟可能都會佔很大的版面」，追問「若此專案項目反覆修改多次…我不想下拉一堆卷軸」。
  三輪提案（一筆一行 → 依階段收合 → A＋C）後他問「最優解是哪一個」，選了 **A＋C**：明細列＝**一個階段一行**（筆數 · 淨效果 · 日期鏈），逐筆明細搬到 **「完整軌跡 ↗」視窗**。
  他明講：**這些不列印**（tooltip 裡的資訊不必做紙本版）、**時間維持完整 `2026-09-13 16:10`**（鏈上的月日 `08-21` 是日期不是時間，他看過範例畫面後認可）。
- 做法（`app.jsx`，後端與稽核列**一個字都沒動**）：模組層 `endOf()`／`shortMd()`／`HOP_GLYPH`／`SYSTEM_NOTE_TYPES`／`hopTitleOf()`／`phaseChainOf()`／`phaseCircleOf()`／
  `groupAdjacentEntries()`（第 35 批的 `changeGroups` 搬出來給視窗用）／`entryFieldChanges()`；App 多一個 `histModal` state（Esc、`openModalCount` 都接上）。
  - 明細列：`[...PHASE_KEYS,'stage']` 每階段一行，**沒有 max-height、沒有捲軸**。淨效果比「最早的原訂（第一筆的 old End）」與「現在」＝ `actualEnd || end`（延期完成的「現在」是實際完成日）。
    鏈上每一跳對應一筆稽核列，值沒變的只印記號（✓ 完成、↶ 撤銷、起 只動開始日、未填 回退清空）。頁尾「最後變更 … 類型（分類）：理由」與「初始時程 時間」**各一行、不截斷**（使用者看到「① E…」後要求「不要用...省略」；原本兩段擠同一行、理由被 truncate）。**完成那一跳一律印完成日**（準時完成的稽核列 old End == new End，原本只印 ✓ → 使用者回報「09-09 → ✓ 只有✓符號沒日期，不直觀」），淨差 0 且最後一跳是準時完成時標「準時完成」而不是「回到原訂日」。
  - 視窗：最新在上、`groupAdjacentEntries` 合併回退的四筆、階段篩選鈕只列有紀錄的階段（帶筆數）、使用者填的理由截一行點開展開（`expanded` 存在 `histModal` 裡，不進 localStorage）、
    系統組的說明（`SYSTEM_NOTE_TYPES`：提早／延期／撤銷完成、重新排程）收成「說明 ⓘ」、初始時程壓成最底一行、通知只在標題計數（第 45 批那一行摘要不動）。
- 實測（`ZZ72-A`，造 10 筆變更：日期異動×3、延期完成、撤銷、規格回退到 ①（4 筆快照）、重新排程；測完軟刪除、回到 62 筆）：
  明細列卡片 **241px**（含通知列與頁尾；舊版三筆就 265px）、① 那行鏈 `08-21 → 09-05 → 08-21 → ✓09-13 → ↶ → 未填 → 09-19 → 09-25`、「7 筆 · 延後 35 天」；
  視窗 507px 高（整個螢幕高）、理由 17px → 點開 33px、篩 ② 只剩回退那筆、Esc 關閉；淺色底的藥丸 `rgb(248,250,252)`／字 `rgb(71,85,105)`。
- ⚠️ 一個 PUT 撞 `PhaseOrderViolations` 400（改 ① End 到 09-19 但 ② 還在 09-08）不是 bug —— 造資料時要先看跨階段順序。
- `npm run build`、`?v=20260913004`；手冊「資料列與明細」多一節「明細裡的『時程變更軌跡』：一個階段一行」、第 10 章通知那段改字、版本對齊。

### 第 71 批（2026-09-12）同一句話第三次 → 準時完成的標籤／提早完成後改 End／④ End 早於 ③ 實際完成日的提示／撤銷 note 補開始日
- 使用者把第 69、70 批那句原話**第三次**一字不差再問。沒有從頭再對第 67～70 批標「不必再對」的規則，只找「三個動作交會之後畫面講的話」還對不上的，
  並對本機 DB 跑 SQL 佐證。列了 🟡2 / 🟢2、沒有 🔴，他**四項全選**。
  1. ✅ **準時完成的紀錄，畫面三處都印「提早完成」**：`ChangeType` 仍是 `提早完成`（第 20 批），但 ✓ 藥丸、軌跡藥丸、`PhaseAuditList` 都直接印 `CHANGE_TYPES` 的 label，
     完成視窗寫「將記為：準時完成」、存完卻變「✓ 提早完成」而次數又沒加。**本機 8 筆 `提早完成` 裡 6 筆是準時**。
     修：`entryLabelOf(h)`（看 `old End == new End`，不看 Note 文字），四處＋撤銷視窗＋ `/done` 回應訊息一起改；稽核列一個字不動。
  2. ✅ **提早完成後解鎖改 End**：延期那條第 21 批有講（清 ActualEnd），提早這條什麼都沒說 —— **本機 NID 77 的 ③ 就是**（提早 09-10 → 日期異動 09-09，✓ 寫 09/10、欄位 09-09、EarlyCount 仍 1）。
     修：PUT `ValidEarlyDoneOfAsync()`（與 `PhaseAlreadyDoneAsync` 同一條基準線）→ `日期異動` 說明接一句；`donePanel` 的 `endModified` 在 ✓ 旁印黃字「（結束日之後已改為 X）」。不擋存檔。
  3. ✅ **`PhaseOrderViolations` 只比原訂 End、不比 ActualEnd**：③ 延期到 09-15，④ End 壓 09-12 存得進去，之後 ④ 只能記成延期。**規則不動**（他選的），只加 `PrevActualHint`（②③④ End 底下、黃字、非阻擋）。
  4. ✅ `/undo-done` 對「被夾過、之後又被改掉的 Start」補一句 note（前端 `startRestore.kind === 'modified'` 原本沒有後端鏡像）。
- **實測**（`t71.js`，造 `ZZ71-A/A2/B/B2/C/D` 六筆、測完全部軟刪除、回到 65 筆）：API 26 案例全過（準時 toast 含「準時完成」不含「提早完成」；提早後改 End 的 note 含「已於 … 標記完成（完成日 …）」且使用者理由仍在；
  沒完成紀錄／撤銷後不加那句；撤銷 note 含「開始日在標記完成之後已被改成 2026-08-30」且 Start 維持；③ End < ② 實際完成日仍 200）。
  瀏覽器（第 59 批繞法）：`ZZ71-A` 編輯視窗「✓ 準時完成 · 09/10」、tooltip 與 `PhaseAuditList` 同字、撤銷視窗「記下的「準時完成」」；NID 77 的 ③ 顯示「（結束日之後已改為 2026-09-09）」黃字＋ tooltip、軌跡上 ① 的藥丸已是「準時完成」；
  `ZZ71-D` 的 ③ End 底下出現 `PrevActualHint`、把 End 改到 09-09 提示消失（取消未存）。
- ⚠️ 測試 C 第一次 400 不是 bug：提早完成後 End 是 09-01，把 Start 改成 09-02 會撞 `InvalidDateRanges`，要改成比 End 早的日子。
- ⚠️ 這次對過、沒問題的（下一輪不必從頭再對）：回退到目前階段而該階段沒日期時的 `wroteAny` 路徑、回退後 `phaseDoneEntry`／`phaseNotifiedEntry` 基準線、
  `backfillCap` 與 `PhaseOrderViolations` 不互撞、`alsoComplete` 的 `phaseDoneEntry` 過濾、完成視窗補登／clamp 提示、撤銷視窗四條、rollback 從 5 退回的 Status 顯示。
- `dotnet build` + `npm run build`、`?v=20260912006`；手冊第 5 章的判定表多一列「準時完成」＋一段「標記完成之後又去改日期」、ChangeType 表補字、版本對齊。

### 第 70 批（2026-09-12）同一個問題再對一次 → 三輪：撤銷 Start>End／撤銷視窗＋回退到目前階段／⏰ 文案＋「補記完成…」
- 使用者把第 69 批那句原話**一字不差再問一次**（「檢查目前的 rule 是否還有缺陷、規格回退時、補登完成日期(非當天)或異動日期時，是否都可以顯示正確對應的資訊」）。
  先確認第 69 批的 build／`?v=20260912003` 都已落地、四項沒有回歸，再從**三個動作互相交會**的地方找（69 批只看單一動作的呈現）。
  列了 🔴1 / 🟡1 / 🟢2，他**只選第 1 項**；2~4 記在下面，沒做。
  1. ✅ **撤銷完成把夾過的 Start 還原，卻不看 End 有沒有一起還原** → `Start > End` → 那筆之後**連改現況描述都 400**（`InvalidDateRanges`），畫面上沒有任何一格解釋得了。
     實測 `ZZ70-A`：① 原訂 09-10 補登 09-01（Start 09-05 夾成 09-01）→ 解鎖 End 改 09-03（`日期異動`）→ 撤銷 → `SpecStart 09-05 > SpecEnd 09-03`、PUT 現況描述 400。
     修：`origStart > restoredEnd` 就不還原、稽核列寫「開始日維持 X（原本的開始日 Y 晚於目前的結束日 Z…）」。`ZZ70-D`（End 有還原）與 `ZZ70-E`（End 改成 09-06 ≥ 原 Start）照舊還原，無回歸。
     第一輪只動後端。
- **第二輪**：他問「我還有哪些要修正的?」→ 依嚴重度重列 7 項（這批的 2~4 ＋ 第 68 批的 5、6 ＋ 匯入 H4/H5 ＋ 事後補記），他選了 **這批的 2 與第 68 批的 5**：
  2. ✅ **撤銷視窗照後端規則講**：`handleUndoDone()` 算 `willRestore`／`endModified`／`startRestore`（restore／blocked／modified）／`actualCleared`
     塞進 `undoModal`，JSX 逐條印。在此之前視窗一律印「結束日由 X 還原為原訂 Y」（End 改過時後端不還原）、開始日隻字未提、延期在 ActualEnd 已清之後仍寫「清掉」。
     瀏覽器實測 `ZZ70-G`：四條 `<li>` 與後端 note 逐字對上（「已被改成 09-03，維持改過的值、不還原」「開始日維持 09-01：原本的 09-05 晚於…」）。
  5. ✅ **規格回退可以以目前這一階段為目標**：後端 `target > curStage` 才 400（訊息改講「後面沒走到的階段直接解鎖清空即可」）；note 與回應訊息在目標＝目前時不印「由 X 回退至 X」；
     前端 `filter(s => s <= curStage)`、那顆標「（目前）」、選中多一行說明；入口鈕仍 `cur < 2` 不出現（StatusID 1 回退到 ① 沒有意義，解鎖改日期就好）。
     API 實測 `ZZ70-F`（14 案例全過，見 `FIELD_SPEC.md`）；瀏覽器實測回退視窗兩顆鈕「1. EMS規格確認」「2. MSD確認中（目前）」、清空清單與「⚠ 未壓日期」那句都跟著目標變。第 68 批第 6 項（H1 訊息）因此自動變對。
  `npm run build`、`?v=20260912004`，手冊第 5／6 章與版本號同步。測完 `node t70b.js clean`，回到 65 筆。
- **第三輪**：他問「建議那些先修?」→ 我建議「⏰ 文案、`DonePastHint` 文案先做；事後補記等他決定；匯入 H4/H5 不做」→ 他說「**1、2、3 都做，4 不用做，可以移除**」。
  ⚠️ 「可以移除」我問過是不是指拿掉匯入功能，他說**不是，只是不做第 4 項** —— 匯入留著。
  1. ✅ ⏰ 三處文案改成「標記完成時填的實際完成日晚於原訂結束日就記一次（補登準時完成不算）」。
  2+3. ✅ **「補記完成…」**（`/done` `backfill:true`，規則與實測全在 `FIELD_SPEC.md`「事後補記」那段）。要點：只收已走過的階段、StatusID/Status 不動、
     不收 alsoComplete、上限多一道 `backfillCap`（下一階段 max(End, ActualEnd)）、稽核 note 接「（事後補記：StatusID 已在 X，不變）」，
     **`/undo-done` 靠 `Note.Contains("事後補記")` 讓撤銷時 StatusID 不退**（沒有加欄位，與「未確認送出」同一種做法；前端撤銷視窗與 tooltip 看同一字串）。
     `DonePastHint` tooltip 改指向這顆鈕；`min > max`（匯入倒序）時鈕不出現、tooltip 講要先修哪兩個日期。視窗預設日期＝原訂日（不是今天）。
     ⚠️ `latestDoneEntry()` 是跨階段 Id 最大 → 補記一筆之後「撤銷」會先輪到它（LIFO），要撤更早的要先撤它 —— 後端同一條 SQL，兩邊一致。
  API 實測 `ZZ70-H`／`ZZ70-I` 27 案例全過；瀏覽器實測從「已略過此階段 ＋ 補記完成…」開窗 → 改日期 → 確認補記 → toast／End／StatusID 都對。
  `dotnet build` + `npm run build`、`?v=20260912005`；手冊第 5 章新增「補記完成…」一節、第 4 節那張灰字訊息表改寫。測完 `node t70d.js clean`，回到 65 筆。
- ⚠️ 這次對過、沒問題的（下一輪不必從頭再對）：`延期完成` 之後改 End → `ActualEnd` 清掉、`日期異動` note 含「先前記錄的實際完成日 X 已一併清除」（`ZZ70-B`）；
  之後撤銷 → note「實際完成日先前已被清過」、`DelayCount 1→0`；提早完成後改 Start（`起日調整`）再撤銷 → Start 維持使用者改的值、End 還原（`ZZ70-C`）；
  主軌跡與 `PhaseAuditList` 對 `提早完成`（含夾 Start 的兩行）／`日期異動`／`撤銷完成`（延期那種沒有欄位變動、只剩 note）的畫法；回退視窗、完成視窗的即時判定與補登提示。
- ⚠️ 測試前先 `preview_stop` → `dotnet build` → `preview_start`（第 68 批那個坑）：這次 server 是 12:51 起的、dll 12:54 才 build，起手就重來一次。測完 `node t70.js clean`，回到 65 筆。

### 第 69 批（2026-09-12）顯示面的檢查（規格回退／補登完成日／異動日期）→ 修 `重新排程` 的第三個側門
- 使用者：「檢查目前的 rule 是否還有缺陷、規格回退時、補登完成日期(非當天)或異動日期時，是否都可以顯示正確對應的資訊」。
  這次只看**畫面呈現那一面**（稽核列寫了什麼 → 軌跡／編輯視窗怎麼畫），跳過第 67／68 批標「不必從頭再對」的規則。
  列了 🔴1 / 🟡1 / 🟢2，他先選 **第 1 項**，接著說「2、3、4 也修正」—— **四項全做**。
  1. ✅ **回退 → 只填 Start 存一次 → 再填 End，記成 `init`**（本機重現 `ZZ69-A`：#516 規格回退 → #517 init(S 09-10, E 空) → #518 **init**(E → 09-12)）。
     第 35→68 批的判定是「這個 phase **最後一筆**是誰」，中間插一筆不動 End 的列（只填 Start 的 `init`、`起日調整`、`通知寄送`）答案就被洗掉 ——
     同一個 bug 這是第三個側門。改問「**End 曾經有過值**」（`PhasesWithEndEverSetAsync()`，`GROUP BY Phase` 一次查四個階段、吃同一個 `tx`），
     `LastChangeTypeByPhaseAsync()` 整支移除。前端 `phaseNotifiedEntry()` 的基準線**刻意不改**（它問的是「通知要不要重問」，不是同一個問題）。
- **實測**（`t69b.js`／`t69c.js`，造 `ZZ69-B~G` 六筆、測完全部軟刪除、回到 65 筆）：回退→Start→End = `重新排程`；回退→一次填 = `重新排程`（無回歸）；
  從來沒有 End → 填 End 仍是 `init`、② 首次確認日仍是 `init`；③ 清空 End（`日期異動`）→ `起日調整` → 重填 = `重新排程`；② 回退→重填確認日 = `重新排程`、① 沒被多寫列。
  掃了本機 268 筆 active 稽核列：**0 筆**既有的這種錯分類，所以沒寫修正腳本（`14_fix_reschedule_changetype.sql` 只認「前一筆是回退」，抓不到這種）。
- ⚠️ 案例 4 第一次用 ① 測失敗（400）—— 不是 bug，是第 42 批「① End 原本有值就必填」擋的；手動清空 End 這條路只有 ②③④ 走得到。
- ⚠️ 這次對過、沒問題的（下一輪不必從頭再對）：補登 Note「（完成日 X，於 Y 補登）」與軌跡「提前／延後 N 天」（前後端都拿完成日算）；
  資料列「→ 實際」與 tooltip 天數；`✓ 提早／延期完成 · MM/DD` 拿的是完成日；準時不印「由 X 更新為 X」；`撤銷完成` 兩種 note；
  回退多階段併一張卡；`dayDiff` 對空值回 null（`重新排程`／`規格回退` 不畫藥丸）。
  2. ✅ 編輯視窗 `PhaseAuditList` 與主軌跡同一組字：`延期完成` 印「結束 原訂 X → 實際 Y」、`撤銷完成` 印「→ 還原為」（`isDelay`／`isUndo`／`arrow`）。
  3. ✅ `/rollback` 多 SELECT 四個 `*ActualEnd`，被清掉的實際完成日寫進**共用的** note：「（一併清除實際完成日：1_EMS規格確認 2026-09-06；延期／提早次數是既成事實，不會跟著回退）」。
     ⚠️ 一定要在迴圈**外**先算好、接在同一句 note 上 —— 逐階段各補會讓四筆快照說明不同、`changeGroups` 併不成一張卡（實測三筆 note `Set` size = 1）。
  4. ✅ 回退視窗「清空後會變成⚠ 未壓日期」那句改成不分目標一律顯示、印目標階段名（原本 `target === 1` 才出現；`target` 開窗時就是 `curStage-1`，不會是空的）。
- **實測 2/3/4**（`t69d.js`，造 `ZZ69-H`：① 延期完成 09-06 → ② 準時 → ③ 壓日期 → 回退到 ①；再 ① 提早→撤銷→再完成→② 完成到 StatusID 3；測完軟刪除、回到 65 筆）：
  API：三筆快照 note 相同且含實際完成日、`delayCount` 不動、沒有 ActualEnd 的第二次回退不加那段。
  瀏覽器（第 59 批那個 `MediaQueryList.matches` 繞法）：① 的異動紀錄印「結束 原訂 2026-09-03 → 實際 2026-09-06」、撤銷那行印「結束 2026-09-11 → 還原為 2026-09-12」；
  回退視窗預設目標 ② 時那句寫「清空後「2. MSD確認中」…」、點 ① 後改成「1. EMS規格確認」。
  ⚠️ `/done` 的 `completedAt` 不可以是未來日 —— 第一次把 ② 的確認日設成 09-13（明天）再 done 就 400，不是 bug。
  ⚠️ 回退視窗那排階段鈕的文字是 `STAGE_CODES` 的「1. EMS規格確認」（點號），不是 `PHASES` 的「1_EMS規格確認」—— 用 regex 找按鈕時別猜錯。
- 手冊：ChangeType 那張表補了 `延期完成`／`規格回退`／`重新排程` 三格的說明字，版本對齊 `20260912003`。`?v=20260912003`。

### 第 68 批（2026-09-12）規則的第三次檢查 → 修完成日下限、重新排程判定、撤銷／回退的狀態比對
- 使用者：「幫我檢查我的 rule 還有那些地方有 bug」。**刻意跳過第 67 批標「下一輪不必從頭再對」的那些**，只找交會處，
  對本機 DB 跑 SQL 佐證。列了 🔴1 / 🟡3 / 🟢2 ＋ 上一輪已知 2 項，他選「**1 → 2 → 3 → 4**」；5、6 進第 4 節等他決定。
  1. ✅ **`/done` 下限是 `Start`，但 `ApplyStartDefaults()` 把沒填的 Start 補成 = End** → 補登提早在大多數資料上做不到
     （SQL 實測 `SpecStart = SpecEnd` **58/62**、Msd 30/53、Uat 41/48；② 沒 Start 欄反而能）。改成 max(前一階段實際結束日, 半年前)，
     前端 `doneMainMin()`／`doneExtraBounds()` 同步、視窗文案只剩 'prev'／'half' 兩種理由，`from:'start'|'today'` 整個拿掉。
     ⚠️ `hasStart` 從 `doneModal` 移除，`plannedStart` 留著給 clamp 提示（一併記錄的列也補了「開始日會一併調整為 X」）。
  2. ✅ **規格回退 → ✉ 通知 → 重壓日期被記成 `init`**：`LastChangeTypeByPhaseAsync()` 沒排除 `通知寄送`（與階段同 phase）。
     同批把「手動清空 End（`日期異動` 新值空白）→ 重填」也判成 `重新排程`；前端 `phaseNotifiedEntry()` 基準線同步。本機 DB 0 筆既有案例（沒寄過信）。
  3. ✅ **前一階段的下限只比原訂 End、只在提早時比** → 「② 延期到 9/10、③ 補登 9/05」放行。`PrevPhaseEndOf()` 改回 max(End, ActualEnd)
     ＋ `IsActual` 旗標；`/done` 讀 `cur` 時**必須 SELECT 四個 `*ActualEnd`**（原本沒讀，改了 helper 也看不到）。提早／延期都套（既有倒序資料 8 筆全是 StageCode 5，按不到）。
  4. ✅ **`/undo-done` 不看呼叫端以為要撤的是哪一筆**：A 看著 ②、B 剛標完 ③ → 撤掉 ③。加 `historyId`（`/rollback` 加 `fromStage`），不符 409 `conflict:true`，
     **沒帶才跳過**（與樂觀鎖同界線）。前端 409 時視窗標題改「資料已被其他人修改」。
- **實測**（`t68.js`，造 `ZZ68-A/B/C/E` + 瀏覽器造 `ZZ68-F`，測完全部軟刪除、回到 65 筆）：36 個 API 案例全過
  （半年前 400／補登 09-01 早於 Start 09-05 → 200、Start 夾到 09-01、Early 1、Note 含「開始日原為」／實際完成日 09-08 擋 09-05 放 09-08／
  舊 `historyId` 409 且資料沒動／`fromStage` 舊值 409／回退→通知→重填 = `重新排程`、手動清空→重填 = `重新排程`、confirm 只剩 1 筆 init／alsoComplete 無回歸）。
  瀏覽器：NID 61（① Start=End=08-21）完成視窗 min **2026-03-12**、文案「最多回推半年」、選 08-15 出現 clamp 提示；`ZZ68-F` ③ 的 min **09-08** 且文案寫「實際完成日」。
- ⚠️ 通知列本機寄不了信，用 sqlcmd 直接 INSERT（ChangeType 用 `NCHAR(0x901A)+…` 避開 Git Bash 的 Big5 問題）。
- ⚠️ `preview_start` 是 `dotnet run --no-build` —— **改了 C# 一定要先 `dotnet build`，而且舊的 process 會鎖住 exe 讓 build 只成功一半**（dll 有更新、exe 沒有）；
  第一次跑測試就是打到舊程式（訊息還在講「開始日是 2026-09-05」）。先 `preview_stop` 再 build 再 start。
- ⚠️ 第 68 批**沒有查到問題的**（下一輪不必從頭再對）：`/done` 的 `newStatus` 連動、`/rollback` 清空範圍、`PhaseClearViolations` 的 n、
  `StatusStageMismatch` 三條路、`undo-done` 的 LIFO 與 `NextPhaseEndOf`、`resolveDuePhase`／`unsetDuePhase`／`getDueEntry`、`DoneOrderHint` 條件。
  另外**沒選、沒做**：匯入沒套 H5（Excel `Done + StatusID 3` 直接寫進去，與 H4 同一家）、匯入倒序資料的 `min>max` 死結、legacy `Done + StatusID<5` 走 `/done` 時 Status 不會被改回 Ongoing（本機 0 筆）。

### 第 67 批（2026-09-11）壓日期規則的第二次全面檢查 → 修「撤銷完成」的倒序防呆
- 使用者要求「重新檢查我目前壓日期的 rule 是否還存在 bug」。逐條對過 `POST`／`PUT`／`/done`／`/rollback`／`/undo-done`
  與前端 `validateEdit`／`donePanel`／`doneMainMin`／`doneExtraBounds`／`isPhasePassed`／`unsetDuePhase`，
  並對本機 65 筆跑 7 條不變量 SQL（Status↔StageCode 矛盾／prefix 洞／缺口／End 倒序／未走完階段有 ActualEnd／Start>End）。
  **DB 只有既知的匯入舊資料**：NID 49 缺 ③；8 筆 StageCode=5 的 End 倒序（16/18/26/27/39/41/42/48）。
- 列了 3 項可修 + 4 項上一輪沒挑的。第一輪他只選第 1 項；接著問「我還有哪些需要修正的」→ 我依嚴重度重列 → **他選了 1、2、3 都修**：
  1. ✅ **撤銷完成把 End 抬回原訂日，卻不看下一階段已壓的日期** → 倒序 → 之後那兩欄互相卡死、`resolveDuePhase` 把「最急」指到後面那一階。
     實測 `ZZ67-A` 確認。做法見 `FIELD_SPEC.md`「撤銷完成」那段：`NextPhaseEndOf()` + 前端 `nextConflict`（視窗標紅、按鈕 disabled）。
     ⚠️ 只在**真的要還原 End** 時驗；`==` 放行；選「擋下」不選「夾」。4 個 API 案例 + 8 個編譯後前端案例全對得上。
  2. ✅ **H5：`Status = Done ⇔ StatusID = 5`**（`StatusStageMismatch()`，見 `FIELD_SPEC.md`「Status」那段與 `CLAUDE.md` H5）。
     在此之前 `PUT` 收 `Done + StatusID 1`（實測 200）、Status 下拉三個值隨便選；`Done` 讓那筆從所有預警消失、KPI 算結案而 StatusID 仍是 1。
     `POST` 一律驗、`PUT` 只在其中一欄被改動時驗；前端 `validateEdit()` 鏡像＋Status 欄就地標紅；**StatusID 調到 5 時一併把 Status 改成 Done**（灰字說出來）。
     API 7 案例（兩個方向 400、沒動不驗 200、`1→5+Done+理由` 200、`/done ④` 無回歸）＋瀏覽器實測（Status 改 Done → 紅框紅字擋存；StatusID 調 5 → Status 自動 Done）。
     ⚠️ 提案時我把它排第一（「靜靜騙到人」那一類）；本機 0 筆矛盾所以他第一輪沒選，第二輪才選。
  3. ✅ `sixMonthsAgoIso()` 夾到目標月最後一天。編譯後 `app.js` 跑 11 個日期（含 8/31、10/31、12/31、閏年 2/29）與 .NET `AddMonths(-6)` 全對上。
  3b. ✅ 順手把上一輪第 5 項也修了：`/done` 下限文案 Start 排未來時改講「開始日 X 還排在今天之後，所以完成日只能是今天」；前端 `doneMainMin` 多一個 `from:'today'`，視窗那行說明同步。
  4. ✅ **`TODAY_ISO` 跨午夜過期**（第三輪，他說「如你建議的順序修正」）：`TODAY`／`TODAY_ISO`／`formatToday` 改 `let` + `refreshToday()`，
     App 每次 render 開頭重算；每分鐘 tick 只在日期字串翻過去時 `setTodayTick`；`dueAlerts`／`dueInfo` 兩個 memo 相依加 `todayTick`（規則見 `CLAUDE.md`）。
     實測：把頁面 `Date` 換成明天、不碰畫面等 98 秒 → 頁首「以今天 … 為基準」自己翻成 09/12；換回來再翻回 09/11。
     需關注 14/5/8 沒變是對的 —— 唯一壓在 09-11／09-18 邊界上的是 NID 4 的 ③，而它的焦點階段是已逾期的 ①。
     ⚠️ `npm run build` 第一次寫 `wwwroot/app.js` 遇到 `UNKNOWN: unknown error, open`（檔案被鎖），等 2 秒重跑就好 —— 不是程式問題，但 build 失敗時產物是**舊的**，一定要看 grep 有沒有新符號。
  剩 **兩項**仍在：匯入倒序資料的 `min>max` 死結（DB 8 筆倒序全是 StageCode 5，踩不到）；
     H4 沒做（`InferStageCode()` 「③ 有日期 → 3」不看 ② 有沒有確認日，匯入當下就能做出 NID 49 那種洞）。兩項與「已略過此階段」事後補記是同一件事的三個面（既有跳空資料的出口），
     **等使用者選 (a) 維持現狀／(b) 事後補記按鈕／(c) 匯入時跑檢查 再一起做**，不要各補一塊。
- ⚠️ 瀏覽器實測走了第 59 批那個繞法（`MediaQueryList.matches` 覆寫成 false ＋兩次 `resize`），`innerWidth` 仍是 0；React 受控 `<select>` 用 `el.value=…; dispatchEvent(new Event('change',{bubbles:true}))` 可以觸發 onChange。測完 `取消` 關窗、沒有存任何東西。
- ⚠️ 這次沒查到問題的：H1／H2／H3、gating、End 遞增、`/done` 鏈式上下限與 `prevAlsoListed`、`PhaseAlreadyDone` 基準線（含 `撤銷完成`）、
  `/rollback`、三對鏡像（`isPhasePassed`↔`StagePassed`、`unsetDuePhase`↔`UnsetPhaseOf`、`extras`↔`alsoStages`）—— 下一輪不必從頭再對。
- ⚠️ 驗證編譯後 `app.js` 時，`eval` 出來的 `const` 不會跨 eval 可見 —— 抽函式要改成 `globalThis.xxx =`。與程式無關，是測試 harness 的坑。

### 第 66 批（2026-09-11）一條不變量：H1 不准清空／挖洞、H2 StatusID 只能往前、H3 StageCode NOT NULL、撤銷上一次標記完成
- 使用者在第 65 批之後問：「可以想到把副作用清除的作法嗎? 幫我想一個都沒有bug的流程」。
  回答的核心是**那個副作用不是顯示規則的問題，是那個狀態根本不該存在** —— 鎖成一條不變量
  （`StatusID = N` ⇔ ①…N-1 全有 End、日期連續前綴、End 遞增），顯示規則就塌縮成一行。
  列了四個洞 H1~H4 ＋ 一個決定（誤按怎麼撤銷），他選 **H1、H3、H2 ＋ 加「撤銷上一次標記完成」按鈕**；
  **H4（匯入後對不變量跑檢查）沒選、沒做**。⚠️ 撤銷方式的兩個選項裡我推薦「就用規格回退」（不加新功能），他選了加按鈕。
- **H1** `PhaseClearViolations()`（後端）＋ `validateEdit()`（前端就地標紅）：已走完的 End 不可清、清空不可挖洞。
  只看「原本有值、這次清空」。⚠️ ① 另有第 42 批的「原本有值不可清」，那條先跑（測試裡 StatusID=2 清 ① 命中的是它）。
- **H2** `PUT` 往回 400；下拉把往回的 option `disabled` 並在文字後註明。往前仍放行（匯入資料的階段多半是填少了）。
- **H3** `17_stagecode_not_null.sql`：本機 65 active + 20 deleted **全部已是 1~5、回填 0 筆**，重跑 idempotent。
  `IsValidStageCode()` 不再接受空值；`PUT` 對空值**不套**「只在被改動時才驗」（DB 寫不進去，與其 500 不如 400）。
  匯入空白 → `InferStageCode()`（Done→5、③ 有日期→3、② 有確認日→2、否則 1；④ 不推），回應 `stageInferred`、toast 要求核對。
  `isPhasePassed()` / `StagePassed()` 的 `legacy` 整段刪除，各剩一行 `stage < StatusID`；「有 ActualEnd 就算走完」留著當保險。
- **撤銷** `POST /api/requirements/{id}/undo-done`（body 選填 note）：只撤最後一筆有效的完成紀錄（跨階段 Id 最大，LIFO）。
  ⚠️ 「有效」的基準線＝同一階段最後一筆 `規格回退`／`撤銷完成` 之後 —— `PhaseAlreadyDoneAsync()` 與前端 `phaseDoneEntry()`
  **兩邊都改成 `IN (規格回退,撤銷完成)`**，少一邊撤銷過的階段就會永遠 409 或永遠顯示 ✓。
  ⚠️ End 在完成之後又被改過就不還原（比對「目前值 == 稽核列的新值」），只在稽核列講；準時完成不減 EarlyCount、
  不印「由 X 還原為 X」（第 59 批那條）。前端只在最後那一筆的 ✓ 旁畫「撤銷」，視窗列出會動到什麼／不會動到什麼。
- **實測**（`ZZ66-C`，測完軟刪除，回到 65 筆；`ZZ66-A` 是第一輪因 curl 中文編碼失敗留下的殘留，也已軟刪除）：
  H1 缺口 400、已走完 400、後面沒有 → 200（清 ③ 要理由，記成 `日期異動`）；H2 400；H3 POST/PUT 空值 400；
  延期撤銷 → ActualEnd 清、DelayCount 1→0、StatusID 3→2；撤銷後 /done 回 200（不再 409）；提早 2 天撤銷 → 確認日 09-03 還原 09-05、
  EarlyCount 1→0；準時撤銷 → EarlyCount 維持 0；沒有可撤銷 400。前端：NID 20 的 ③ ✓ 旁有「撤銷」、視窗文案正確；
  手動 StatusID 下拉 1~3 `disabled`；NID 4 解鎖 ② 清空 → 儲存被擋、欄位紅字「清空會留下缺口（3_MSD開發中 仍有日期）」；資料沒動。
- ⚠️ **curl 在 Git Bash 送中文 JSON 會被燒成 Big5 而 400**（`Cannot transcode invalid UTF-8`）—— 測試腳本的 note／負責人一律用 ASCII。
- ⚠️ `python` 在這台是 Windows Store 的 stub，什麼都不做也不報錯 —— 改檔一律用 node。

### 第 65 批（2026-09-11）「走完了沒」只看 StatusID，日期反推只留給 StatusID 空白的舊資料
- 使用者附截圖（NID 4：StatusID 1、規格回退後 ①②③ 一次壓成 09-09／09-10／09-11）：
  「**我沒有半個欄位標記已完成，為什麼提示快到期的會是第三個欄位?**」
- 根因：`isPhasePassed()` 那兩個「① 一旦 ② 有日期就算走完、② 一旦 ③ 有日期就算走完」的補救條件，
  註解寫著「StageCode 空的舊資料靠這兩個」，**程式卻對每一筆都生效**。①② 被日期反推成走完，
  只剩 ③ → 「今天到期」；而編輯視窗的 `savedStage()` 只看 StatusID，把 ① 標成「可以標記完成」——
  同一列兩邊講的不是同一件事。**「②③ 的日期是做到這裡才會排」自第 60 批起就不成立了。**
- 修正：`legacy = stageNum === 0`，反推只在 `legacy` 時生效；後端 `StagePassed()` 同步（鏡像）。
  ③④ 刻意沒有反推條件的那段理由**沒動**。
- 實測（本機 65 筆，改前／改後兩套規則對每一列跑 `resolveFocusPhase`）：**只有 NID 4 改變**
  （③ 09-11 今天到期 → ① 09-09 逾期 2 天），一般模式三格分別是「逾期 2 天／逾期 1 天／今天到期」，
  需關注 14 件。**目前 65 筆的 StageCode 沒有一筆是空的**，所以那兩個反推條件現在實際上一筆都碰不到。
- ⚠️ 副作用（已寫進 `FIELD_SPEC.md`）：StatusID=2、② 沒日期、③ 有日期的跳空資料，以前判「② 不用壓」、
  現在會標「⚠ 未壓日期」。第 33 批的註解曾把這種列當成刻意的例外 —— 但那個例外與這次的 bug 是**同一條反推**，
  留一邊就等於留另一邊。當天資料裡沒有這種列。
- 手冊第 10 章「什麼情況不預警」的第二條改寫（原本的例子「① 的日期是去年，但 ② 早就確認了」正是這個錯誤前提）。

### 第 61 批（2026-09-10）壓完成日期那條路的 bug 檢查與修正
- 使用者要求：「幫我檢查我目前壓完成日期的規則是否有 bug」→ 逐條讀完第 58／60 批的前後端，
  回報 1 個會擋到人的 + 3 個較窄的，他選「**修正 1→2→3**」。第 4 項（匯入倒序資料造成
  `min > max` 的死結）與第 5 項（`TODAY_ISO` 跨午夜過期）**他沒有挑，沒有做**。
- **修正 1（真的會擋到人）**：主要階段的完成日下限被「就在同一次 `alsoComplete` 裡的前一階段」
  的**原訂日**抬高 —— 第 60 批想解決的那個情境自己撞牆。② 原訂 9/08、③ 原訂 9/15，
  而 ③ 其實 9/05 完成、② 是 9/03 完成的：日期欄連 9/05 都選不到，
  **擋住他的正是他在同一次送出裡要覆蓋掉的那個值**。
  · 後端把第 22 批那道 `PrevPhaseEndOf` 檢查**搬到 `alsoStages` 驗證之後**（`prevAlsoListed`）——
    搬順序是必要條件，不是整理版面。
  · 前端 `doneModal` **不再存 `min`**，改存 `prevKey` / `plannedStart`，下限由 `doneMainMin()`
    每次 render 重算（勾選是開窗之後才動的）。畫面上要寫出「已勾選一併記錄 X，改由它的完成日
    約束先後順序」—— 下限會鬆掉的那一刻更要說得出「為什麼剛才選不到、現在選得到」。
  · ⚠️ 拿掉之後順序**仍然成立**，靠的是鏈式上下限（前一階段若在清單裡必然是最後一筆，
    上限＝主要階段的完成日）—— 這是**證明**，不是放寬。
- **修正 2**：一併記錄的階段回 409／400 會把「使用者真的按下去的那一個」一起打掉。
  改成「已經有完成紀錄」與「StatusID 已經走過」兩種都**跳過該筆、照樣完成主要階段**，
  回應訊息與 `alsoSkipped` 要講出跳過了哪幾階。⚠️ 排在主要階段**後面**的仍然回 400
  （那不是畫面舊了，是請求本身就不對）；⚠️ **主要階段自己的 409 一個字都沒動**。
- **修正 3**：「原訂日排在未來的不收」第 60 批只寫在 `app.jsx` 的 `extras` 過濾裡，
  後端整個沒驗 —— 而同一批的規格明寫著「後端每一筆自己再驗一次」。
- **實測（造 5 筆 `ZZ61-*`，測完全部軟刪除，回到原來的 65 筆）**：
  修正 1 改前 400／改後 200，落地 `MsdConfirm 09-03 ≤ MsdEnd 09-05`、`EarlyCount` 0→2、
  稽核列 confirm(343) 排在 msd(344) 前、PUT 回寫 200（沒有被 `PhaseOrderViolations` 鎖住）；
  前一階段**不在**清單裡時仍回 400（guard 還在）。修正 2 回 200 且 `alsoSkipped:["2_MSD確認中"]`、
  ② 沒有多寫一筆稽核列。修正 3 回 400。不帶 `alsoComplete` 的路徑無回歸。
- **前端驗證方式**：瀏覽器分頁這次整個沒回應（`read_page` / `find` / `javascript_tool` 全部逾時，
  主控台沒有任何錯誤），改成 **直接把編譯後 `wwwroot/app.js` 裡的 `doneMainMin` 抽出來在 node 上跑**
  7 個案例（含「勾/不勾」「Start 排在未來」「confirm 沒有 Start」）全過。
  ⚠️ 重點是**測編譯產物、不要重寫一份鏡像** —— 重寫的話測到的是我寫的那份，不是瀏覽器跑的那份。
- ⚠️ 順手發現、**沒有改**：`/done` 的下限錯誤訊息在「Start 排在未來被夾到今天」時，
  仍然印 `Start` 的值（「完成日不可以早於 2026-09-15」），而實際生效的下限是今天 ——
  屬於第 58 批的既有文案，這一批沒動到它。

### 第 60 批（2026-09-10）跳過中間階段時，可以在完成視窗上一併記成完成
- 使用者附截圖：「我還沒填寫 2_MSD確認中的已完成日期，直接點選 3_MSD開發中 的已完成日期 →
  目前 2_MSD確認中 會自動顯示已略過此階段的訊息、我是否可以把上述原本壓的 2026/09/08 變成已完成日期會比較好」。
- ⚠️ **先更正了一個前提**：他以為「一定要用回退」。實際上有三條路，而**畫面上指過去的回退是最糟的一條** ——
  回退到 ② 會清掉 `MsdConfirm/MsdStart/MsdEnd/MsdActualEnd/UatStart/UatEnd/UatActualEnd`（含剛按完的 ③）、
  `RollbackCount +1`，重壓後再按一次 ③ 完成又讓 `EarlyCount` **+1 第二次**。
  **第 21 批那條規則是為了防計數灌水而立的，它指過去的替代方案卻是唯一真的會灌水的做法。**
- ⚠️ **我第一次讀錯了他的意思**（以為要「事後補一顆補記按鈕」），他澄清後才知道是**預防**：
  按下 ③ 的**當下**就一併處理。他的做法反而小得多也安全得多 ——
  那時 StatusID 還是 2、② 根本還沒「走過」，**第 21 批那道 guard 連觸發都不會觸發**。
- **實測（T60-1，已刪除）**：② 記 09/08（＝原訂日）+ ③ 提早完成 09/10 →
  `StageCode` 2→4、`MsdConfirm` **仍是 2026-09-08**（一個字都沒變）、`EarlyCount` 0→1（③ 的）、
  `DelayCount` **0**。稽核列 id 309（confirm）排在 310（msd）**前面**。
  理由：完成日 == 原訂日走「準時完成」，而 `days == 0` 時 `setCount` 是空字串（第 20 批）。
- **實測（T60-3）**：把 ② 改成 09/09（原訂 09/08）→ `MsdConfirmActualEnd = 2026-09-09`、
  `MsdConfirm` 不動、`DelayCount` 0→1。**所以日期一定要可以改** ——
  記成準時會讓 `DelayCount` 少算一次，**多報只是難看，少報是把一次延期整個抹掉**。
- **實測無回歸**：不帶 `alsoComplete` 打 `/done` 行為與第 59 批完全一樣（`alsoCompleted: []`）；
  StatusID=1 的需求按 ① 完成時視窗上**不出現**那一區。
- **實測前端鏈**：主要日期改 09/09 → ② 的 max 立刻跟著變 09/09；② 設成 09/10 → 標紅「⚠ 需在 … 之間」
  且「確認完成」變灰；取消勾選 → 日期框 disabled、寫出「不記錄 → 這個階段會顯示『已略過此階段』」、按鈕恢復。
- ⚠️ **使用者選擇「先不動」的部分**：已經卡成「已略過此階段」的**既有資料**沒有補救入口
  （提過的「事後補記完成」按鈕**沒有做**，見第 3 節）。匯入舊資料的處理他選了「一律開放，靠視窗把關」，
  但那條路屬於事後補記，這一批也沒做到。
- ⚠️ 順手發現、**沒有改**：準時完成的 `ChangeType` 是 `提早完成`（第 20 批的設計），
  所以視窗上寫「準時完成（不計次）」、存完之後那顆藥丸卻是「✓ 提早完成 · 09/08」。
  這是**既有行為**（任何準時完成都一樣），改它會動到 `CHANGE_TYPES` 的顯示字，
  而那組字同時用在圖例、篩選、晶片、統計（第 37 批：同一個概念只能有一組字）。

### 第 59 批（2026-09-10）「已完成的結果」與「要按的動作」長得太像
- 使用者附截圖問：「**提早完成的圖示跟完成的圖示看起來都差不多**…目前的顯示方式是否容易讓人混淆狀態?」
- ⚠️ **不是感覺問題，是同一組樣式**：結果標籤 `--tone-good` / `rgba(15,118,110,0.1)` / `✓` / 無邊框；
  動作鈕 `--tone-good` / `rgba(15,118,110,0.08)` / `✓` / 0.3 alpha 邊框 ——
  **同色、底色差 0.02 alpha、同一個 ✓**，唯一差別是一條幾乎看不見的邊框。
- ⚠️ 根本的矛盾：**`✓` 與 teal 是「已經完成」的語言**，卻用在「還沒完成、請你來做」的按鈕上。
  第 58 批讓它更嚴重 —— 按下去不再是「確定嗎」，而是一件要填日期的真工作。
- **做的**（使用者從三個選項挑了最完整的那個）：①動作鈕改 **indigo + 去 ✓ + 「標記完成…」**；
  ②結果標籤維持 teal ✓ 並**補上完成日**（`✓ 提早完成 · 09/02`，只印 MM/DD，完整日期在 tooltip）——
  第 58 批之後完成日可以自填，畫面上卻看不到記成哪一天。**三個維度一起拉開：顏色／圖示／文字。**
- ⚠️ 改了按鈕名就把畫面上每一處提到它的字一起改掉（`app.jsx` 12 處、手冊 6 處的「✓ 完成」）——
  第 37 批那條「同一個概念只能有一組字」，否則使用者會去找一顆不存在的按鈕。
- **順手修掉兩個文案 bug**（都是打開來看才發現的）：
  ①「② 沒有 Start、但下限被前一階段 End 抬上來」會顯示「可選 2026-09-02 ~ …。**這個階段沒有開始日，所以最多回推半年**」
  —— 兩句話自己對不起來（半年前是 03-10）。改成講實際生效的那一個下限。
  ②準時完成時印「確認日會由 2026-09-05 **更新為 2026-09-05**」—— 同一個日期寫兩次，而那正是補登最常見的情況。
  改成「確認日維持 2026-09-05 不變」。
- **沒有動 `Program.cs`、沒有 SQL 腳本。** `?v=20260910004`。CLAUDE.md 與手冊已同步（手冊新增「先分清楚：藍色的是要你按、綠色的是已經完成了」）。
- **實測**（終於把第 58 批那個視窗打開來看了）：
  兩顆並排 → 結果 `✓ 提早完成 · 09/02`（teal `rgb(45,212,191)` / `rgba(15,118,110,0.1)` / 無邊框）、
  動作 `標記完成…`（indigo `rgb(99,102,241)` / `rgba(99,102,241,0.16)` / indigo 邊框）；
  完成視窗：`min=2026-09-02`（**被前一階段 End 抬高，正是設計的那道下限**）、`max=2026-09-10`、預設今天、autoFocus ✓；
  改日期即時翻轉 **提早 5 天 → 提早 13 天 → 準時完成**，補登提示只在「不是今天」時出現；
  延期分支：「延期 5 天完成 / 原訂保留不變 / 延期次數 +1」；
  **端到端送出**（原訂 09-05、今天 09-10、填 09-05）→ 稽核列「準時完成（完成日 2026-09-05，於 2026-09-10 補登）」、
  `delay 0`、`confirm` 仍是 09-05、StatusID 推進到 3。測試資料 2 筆已刪除、複驗 0 殘留。
- ⚠️ Browser 面板仍然是 `innerWidth = 0`（第 5 節那個坑）。**這次的繞法記下來**：
  `Object.defineProperty(MediaQueryList.prototype,'matches',{get(){return false}})` + 兩次 `dispatchEvent(new Event('resize'))`
  → `narrow` 變 false → 16 欄、操作欄回來，就能走完整條 UI 路徑。**幾何量測在這個狀態下仍然沒有意義**（寬高是壞的）。

### 第 58 批（2026-09-10）完成日改成使用者自己填（補登不再被算成延期）
- 使用者原話：「使用者常常沒有完成之後立馬到網頁平台點選完成…**實際上使用者已於 2026/09/09 準時完成了，
  但我可能 2026/09/20 才想到要來平台更新狀態**…反而會顯示逾期的狀態，**這是有問題的!!**」
- ⚠️ **根因**：`/done` 把完成日寫死成 `DateTime.Today`。後果不只是那一列顯示錯 ——
  `DelayCount +1` 是**主管在看的數字**，等於灌了假的延期。
- ⚠️ 順帶更正他的一個誤解：鈕只有一顆「✓ 完成」，**提早／延期是按下去之後由日期算出來的**，
  他的例子其實會判成「延期」（不是他以為的「被迫按提早完成」）。
- **做的**：`DoneRequest` 加 `completedAt`；前端「✓ 完成」改成開一個帶日期欄的視窗（`doneModal`，預設今天）
  並**即時顯示會被記成什麼**（準時／提早 N 天／延期 N 天、End 會不會被改、要不要 +1）。
- ⚠️ **沒帶 `completedAt` 時退回今天** —— curl／測試腳本行為完全不變（已實測）。
- ⚠️⚠️ **範圍後端自己再驗一次**（那個值直接決定 EarlyCount / DelayCount，不可以只信前端）：
  ①不可未來；②下限＝Start（**Start 排在未來時夾到今天**，否則下限 > 上限、一天都選不到）、
  沒有 Start（② 或沒填）→ **半年前**；③提早時仍擋「早於前一階段 End」（沿用第 22 批的倒序防呆）。
  ⚠️ 半年一律用**月**不可用 180 天：前端 `sixMonthsAgoIso()` 與後端 `AddMonths(-6)` 是鏡像，
  用天數兩邊會差到 2 天（實測兩邊今天都給 2026-03-10 ✓）。
- ⚠️ **選的不是今天時稽核 Note 一定要標「補登」** —— 完成日開放自填之後「延期」是可以被寫成「準時」的，
  那一行是日後唯一查得到「誰、什麼時候、補登了哪一天」的地方。
- **有動 `Program.cs`**（`/done` 端點 + `DoneRequest`）、沒有 SQL 腳本、沒有動任何既有規則
  （重複完成／前置階段／已走過的階段／StatusID 推進／準時不計 EarlyCount 全部照舊）。`?v=20260910001`。
- **實測（API，7 case，測完 4 筆暫時資料已全部軟刪除、複驗 0 殘留、回到 64 筆）**：
  ①未來日 → 400；②早於 Start → 400 且指名 Start；③格式錯 → 400；
  ④② 沒有 Start 又超過半年 → 400 且指名 2026-03-10；⑤提早但早於前一階段 End → 400（倒序防呆）；
  ⑥**使用者情境**（原訂 09-09、今天 09-10、填 09-09）→ `提早完成 / days 0`、**delay 0**、
  `SpecEnd` 仍是 09-09（沒被改成 09-10）、稽核 Note =「準時完成（完成日 2026-09-09，於 2026-09-10 補登）」；
  ⑦不帶 `completedAt` → 與改之前一模一樣（延期 1 天、ActualEnd 09-10、delay +1）——
  **同一筆資料同一天，舊路徑 delay 1、新路徑 delay 0，這就是這批的價值**。
- ⚠️⚠️ **前端那個視窗的「畫面」還沒有實測過**（邏輯與後端對得起來，但沒有真的打開來看）——
  Browser 面板整場都在鬧：`window.innerWidth` 變成 **0**（→ 判成窄螢幕 → 精簡模式 → 操作欄整欄沒有，
  一度看起來像「編輯鈕不見了」的 bug），接著 `resize_window` / `navigate` 全部 timeout、
  最後連 300 秒都回不來。**那不是程式的問題**（memory 第 5 節早就記過 0x0 這個坑）。
  中途出現的 `Should not already be working.`（react-dom）也是那個壞掉的 renderer 丟的。
  **下次接手第一件事：把完成視窗打開來看一眼**（日期欄的 min/max、即時判定那段文字、補登提示）。

### 第 57 批（2026-09-07）「未壓日期點不動」不是壞掉，是精簡模式 —— 補上 tooltip 說明
- 使用者：「我之前不是已經有修正未壓日期的部分…**這邊怎麼失效了?**」並附截圖。
- **查證結果：程式完好，沒有回歸。** 實測一般模式（16 欄）4 顆都是 `<button>`／`cursor: pointer`／
  `aria-label="壓定「4_EMS驗收」的日期"`，點下去照樣開視窗並聚焦 `uat` 的 End（NID 6、列數 19→19 沒被展開）；
  精簡模式（9 欄）4 顆是 `<span>`／`cursor: help`。
- ⚠️ **根因是「兩種模式的徽章長得一模一樣」** —— 第 54 批我依他 9/5 的「主管瀏覽時使用精簡模式，
  不需要看到編輯或刪除的功能」把這顆也關掉了，但**畫面上沒有任何地方說得出這件事**，於是看起來就是功能壞了。
  ⚠️ 這是這個專案一路在防的那種靜默落差的**變形**：不是「失敗被吞掉」，而是**「刻意的限制沒有講出來」**。
- **使用者當天確認「不要，維持現狀」** —— 精簡模式維持唯讀，**不要再提把它做成可點**（見第 3 節那條）。
  所以這批**完全沒有動行為**，只在精簡模式那顆 `<span>` 的 tooltip 後面加一句
  「（精簡模式是唯讀檢視，這顆點不動。關掉精簡模式後點它，就會直接開啟編輯視窗並跳到這一階段的日期欄）」。
- 實測：兩種模式的徽章都是 **62 × 21px**（加字在 tooltip 裡，**版面一格都沒動**）。`?v=20260907002`。
  手冊那段改寫成警示框，並補上「1024px 以下會強制精簡、開關會反灰」與「✉ 兩種模式都能按」。
- ⚠️ 驗證時又踩到第 5 節那個坑：改了模擬視窗寬度但**隱藏的分頁不送 `resize`**，React 的 `narrow` 停在舊值，
  畫面卡在「✓精簡模式 · 窄螢幕」且開關 disabled —— 一度看起來像「關不掉精簡模式」的 bug。
  `dispatchEvent(new Event('resize'))` 之後立刻回到 16 欄。**真實瀏覽器不會有這個問題。**

### 第 56 批（2026-09-07）移除 `ctl-mute` —— 原生下拉的選項清單被 `transparent` 畫壞
- 使用者回報「**下拉的視窗顏色跑掉了**」並附工具列截圖。問了之後確認是**兩個都有問題**：
  展開後的選項清單顏色不對，**而且**收合狀態那四顆看起來像被停用。
- ⚠️⚠️ **根因：`<select>` 展開後的 popup 是瀏覽器自己畫的，它拿 `<select>` 自己的 `background-color` 當底。**
  第 50 批的 `ctl-mute` 給沒生效的四顆套了 `background: transparent`，等於沒有底色可用 ——
  popup 落回系統淺色底，而 `select option` 又被指定成深色底淺色字，兩邊對不起來。
  實測：四顆 mute 是 `rgba(0,0,0,0)`，生效那顆 `ctl-on` 是實心 `rgb(79,70,229)` —— **只有沒生效的四顆會壞**。
  ⚠️ 這與第 27 批「凍結欄的底色必須不透明」是**同一類坑**（原生控制項／疊在別的東西上面的元素，底色一律實色）。
- 第二個毛病：`border-color: transparent` + `--text-muted` 讓那四顆**看起來像被停用**，而它們可以點。
- **做的**：`ctl-mute` 從 `input.css` 與 `app.jsx` **整個移除**（CSS 那段改成一則說明為什麼不要做回去的註解）。
  五顆一律吃 `.ctl` 的實心底（`--bg-input`）與可見框線；生效那顆仍套 `.ctl-on`（藍色實心）——
  **「哪一顆在過濾」的訊號本來就由它負責，不需要靠把其餘四顆調暗來對比。**
- **沒有動 `Program.cs`、沒有 SQL 腳本、寬度一格都沒動。** `?v=20260907001`。CLAUDE.md 已同步（第 50 批那條的 ③ 劃掉）。
- 實測（1440／1366／1280 三個寬度，工具列都是 **60px 單行**、`docOverflow` 0、0 console error）：
  深色 → 四顆 `bg rgb(15,23,42)`（**不透明**）／`border rgb(51,65,85)`（看得見）／`color rgb(203,213,225)`，
  option `rgb(30,41,59)` + `rgb(241,245,249)`；淺色 → 四顆 `rgb(248,250,252)` / `rgb(226,232,240)` / `rgb(51,65,85)`，
  option 白底深字；生效那顆兩種佈景都是 `rgb(79,70,229)` + 白字。五顆寬度全部維持 140px。
- ⚠️ 排查過程中被 transition 騙了一次，已寫進第 5 節（`getComputedStyle` 會回動畫中途的值）。

### 第 55 批（2026-09-05）搜尋加上「需求補充」，並把比對範圍寫在畫面上
- 根因不只是漏一欄，是**範圍說不出來**：`remark` 在明細看得到也印得出來，使用者拿裡面的字去搜卻是 0 筆，
  而 placeholder 只寫「NID、項目、負責人...」，那個「...」還暗示著不只這些。
- 現在七欄：`nid`／`mainCat`／`subCat`／`emsOwner`／`msdOwner`／`currentStatus`／`remark`，完整清單掛在搜尋框的 `title`。
- ⚠️ **`notesLink` 刻意不收**（存的是網址，只會撞到網域這種到處都有的字）；
  **日期與 `Status`／`StatusID` 也不收**（那是漏斗＝欄位篩選的守備範圍；打 `2026` 會全中）。
- 實測：`unhold` 0 → **1 筆**；`CMS` 3 → **4 筆**，畫面 NID 與預期集合一致。

### 第 54 批（2026-09-05）「⚠ 未壓日期」徽章改成可點；匯出講明不吃篩選
- 徽章原本是 `cursor-help` 的 `<span>`，而旁邊的 `✉`（催**別人**壓）反倒是唯一按得動的東西 ——
  「自己去壓」得關掉精簡模式 → 找那一列 → `✎` → 在四個區塊裡自己找出是哪一階段，而 `unsetDuePhase()` 早就算出來了。
- ⚠️ **就地換元素，class 與 style 一個字都不改**（實測兩者都是 61.99×21px），目標欄位一律是該階段的 **End**（`data-ct-focus`）。
- ⚠️⚠️ 聚焦的 effect **必須宣告在「沒人接手就聚焦視窗容器」那個 effect 之後**（同吃 `openModalCount`，
  React 依宣告順序執行，寫前面焦點會被搶走）。用 **ref 不用 state**（state 會讓編輯視窗多 render 一次），進 effect 立刻清成 null。
- ⚠️ **精簡模式刻意不傳 `onSetDate`**（見第 3 節那條否決）。
- 匯出：`/api/export` 只有 `WHERE IsDeleted = 0`，而那行字原本寫「把**目前的資料**下載成 .xlsx」，
  正上方才剛寫著「顯示 19 / 64 筆」。⚠️ 筆數走 `requirementsData.length`，**不可用 `sortedData.length`**（那正是不會被匯出的數字）。

### 第 53 批（2026-09-05）統計報表：人員負載與各年月案件數上下對調
- 使用者：「這樣**上半部都是有連動的，下面才不連動**」。順序＝KPI → 風險預警 → 交叉表（帶區間）→ 趨勢圖 → **人員負載（不分區間）**。
- 通則：**同一頁上有兩種口徑時，先把同一種口徑的排在一起、再標示差異**；夾在中間會讓人以為「這張壞了」。
- ⚠️ 換位後要回頭確認指路文字（「同下方趨勢圖」「區間與上方統計表連動」）仍然成立。

### 第 52 批（2026-09-05）刪掉未壓日期那格重複的第三行 ＋ 人員負載補「不分區間」
- 精簡模式 `unset` 分支無條件印階段名，而它**必然**與右邊 StatusID 欄一模一樣（實測 4 筆全中）——
  同一支函式底下早就有「不重複印階段名」的規則，**只有這個分支漏套**。三行 57px → 兩行 39px、列高 78 → 60px、欄寬 96px 沒變。
- 人員負載＝「**現在**誰身上壓著幾件」的當下快照，而區間是依**註冊年月**分組，套上去會變成混合數字、
  兩側加總也不再等於「進行中」KPI。**邏輯對，錯的是沒標示** —— 它前後兩張卡都跟著區間，只有它什麼都沒寫。

### 第 51 批（2026-09-05）修正第 50 批把階段那一排撐成兩行
- 使用者截圖：「**變成兩行了!! 請修正!!**」。需求 1218 → 1384px，1440 螢幕當場斷行；
  **我當時只量了 1280（本來就兩行）與 1600（塞得下），漏掉 1440。**
- 三刀：①圖例改**純圖示 34px**（`ctl ctl-icon`，**不可改回帶文字的 ToggleChip**）；
  ②條件晶片**不併進那一排**（工具列那顆藍色的「進度：進行中 (19)」自己就是移除的入口，再放一顆是同一概念的兩組字）；
  ③右側兩條 `ctl-div` 分隔線移除（含 gap 18px）。改完 need **1224px**，1280/1366/1440/1600 全單行。

### 第 50 批（2026-09-05）把第一列資料上方的 489px 砍到 363px
- **先量再答**（這批的關鍵）：頁首 65 + 工具列 60 + 階段卡 60 + 晶片卡 49 + 圖例列 61 + 兩層表頭 79 + 四道間距 64。
- 做的三項：①圖例預設收起（`ct.legendOpen`）；②只剩預設晶片時整列不出現；
  ③~~沒生效的篩選下拉降成 `ctl-mute`~~ → **第 56 批整個移除**（見最上面那批），①②仍然有效。
- ⚠️ **收起的是螢幕不是紙**：`.legend-strip.is-collapsed` 與 `.chips-print` 在 `@media print` 裡回來，
  且那兩條 CSS **一定要寫在 `@layer` 外面**（unlayered 才贏得過 Tailwind 的 `flex`，寫進 layer 是靜默失效）。
- ⚠️ `historyError` 時強制展開圖例並停用開關（紅字就掛在圖例列裡）。
- ⚠️ 晶片標記抽成 `renderChip()` 一份共用；併排時晶片卡保留在 DOM 專供列印，另一顆標 `no-print`（否則印兩份）。

### 第 49 批（2026-09-05）需求列表預設「只看進行中」
- 63 筆裡 45 筆是 Done —— 預設畫面 71% 是今天不會處理的。用現成機制做，**不加新控制項**（分段控制被否決，見第 3 節）。
- ⚠️ 白名單要含 `All`、網址 `prog` **兩個方向都寫**，否則移掉晶片後重整它又自己回來。
- ⚠️ **StatusID 那排的數字一律不含進度篩選**（`matchExceptStage(item, ignoreProgress)`）——
  含進去的話 `5 結案` 永遠是 0，一顆寫著 0、按下去也沒東西的按鈕比看不到更糟。
- 兩條連動讓每個數字剛好等於「按下去會得到幾筆」：`ALL` 同時清階段與進度；選一個被進度整群擋掉的階段時自動解除進度篩選。
- ⚠️ **搜尋要穿透**（`searchBlockedCount` → 「另有 N 筆已完成的需求符合…／一併顯示」），但
  **不可做成「一打字就自動改成全部」** —— 筆數自己跳動會讓人分不清當下的範圍。

### 第 48 批（2026-09-05）「逾期優先」預設開啟
- ⚠️ **第 23 批那條禁令沒被違反**：它禁的是「兩個偏好共用一個 key」與「關掉之後重整又自己回來」，不是「不可持久化」。
- 自己的 key `ct.duePriority`（沒設定過＝開）；**只有使用者親手按的兩個入口才寫入**，程式設的一律不寫。
- ⚠️ 程式「歸位」一律回 `readDuePriorityPref()`，**不可寫死 `false`**（否則點一張 KPI 卡會變成把偏好悄悄關掉的隱藏開關）。
- ⚠️ 網址 `dp` 兩個方向都寫（預設值來自 localStorage、每台機器不同）。

### 第 47 批（2026-09-05）字級放大時頁首與 sticky 表頭之間漏出半截資料列
- 使用者：「在捲動資料列時出現這種畫面**看起來像是網頁哪邊壞了**」。
- ⚠️⚠️ **根因：`sticky` 的 `top` 是在 zoom 之後的座標系裡算的，而頁首在 `<main>` 外面。**
  `offsetHeight` 是元素自己座標系的值（放大後不變），拿去當 `--head-top-group` 就會差。實測縫：115% → 9.75px、130% → 19.5px。
- **跨 zoom 邊界一律用 `getBoundingClientRect()` ÷ 倍率**；群組表頭也要改（`offsetHeight` 會四捨五入，38.93 → 39）。
- ⚠️ 刻意讓表頭往上多疊 **0.5px** —— 這種對位**寧可疊、不可留縫**。相依陣列要含 `presentZoom`。

### 第 46 批（2026-09-05，共四段，前兩段已全部撤掉）
- **第一段**：`cycleUiScale` 放大前先探一次 overflow + 「⚠ 右邊被切掉」改成橫捲後的浮動鈕（`scrolledX` 存**布林不存捲動量**）。
- **第二段（已撤）**：量到放不下就自動收欄。診斷仍然有效並保留：`narrow` 猜的是「螢幕多大」（寫死 1024），
  而該問的是「16 欄放不放得下」—— **1024～1281 之間有一整段沒人管的空白帶**（1440 螢幕縮放 110~140% 全在裡面）。
- **第三段（現行解法）**：⚠️⚠️ **壞掉的從來不是表格，是框架。** `<header>`／`<main>` 寬度＝視窗寬，
  表格更寬時只是溢出去，往右捲就看到「頁首白底、卡片切在半空中」。
  解法 `.page-shell { width: fit-content; min-width: 100% }`。⚠️ **不可寫死寬度**；⚠️ 列印時要還原成 `width:auto`。
  實測 1152 視窗：shell/header/main 三者都是 1306px，捲到底 `header.right == main.right == 視窗寬`。
- **第四段**：「⚠ 右邊被切掉」**只留給投影模式**。使用者：「有機會可以修正到完全不出現嗎? **嚴重影響操作的 UX 體驗**」——
  浮動那顆正好蓋在編輯／刪除鈕上面。第三段之後橫捲已是完整畫面、瀏覽器捲軸自己就在說「右邊還有東西」。
  ⚠️⚠️ 投影是**唯一例外**（「台上的人看自己的螢幕，不會發現布幕右邊少了幾欄」）。
  ⚠️ 投影下那個數字會**略為低估**（131 vs 實際 267，按鈕自己的寬度會再撐寬 fit-content）——
  **不要把 `clipPx` 加進相依陣列去修正**，那會做出「有按鈕就溢出 → 隱藏 → 不溢出 → 顯示」的無窮翻轉。

### 第 45 批（2026-09-03）通知紀錄不再洗版「時程變更軌跡」
- 實測 7 筆通知：軌跡總高 365 → **951px**，通知獨佔 586px（62%），而面板可視高度只有 224px。
- `通知寄送` 抽出時間軸、收成上方一行摘要（收合 47px／展開 177px）。
- ⚠️ 第 35 批的 `changeGroups` 對它**完全沒用**（合併條件含時間，而每次通知時間必然不同）。
- ⚠️ **精簡的是顯示不是紀錄**：稽核列一筆沒少、完整 Note 掛在 title，但「未確認送出」**一定要顯示在畫面上**。
- ⚠️ 空不空要看 `hasTimeline`，**不可再用 `hasHist`**（「只有通知、沒有時程變更」會畫出空白捲動區）。

### 第 44 批（2026-09-02）寄信邏輯體檢（使用者從 A~F 挑了 A＋B）
- **A**：⚠️⚠️ **`SmtpClient.Timeout` 對 `SendMailAsync` 完全無效** —— 實測設 3 秒、對「接受連線後不回話」的假 relay，
  **60 秒仍未返回**。第 40 批那個 TcpClient 探測**攔不到這一種**（它只管連線建立）。
  後果是前端 fetch 無限等 + `runExclusive` 鎖死整個分頁。用 `SendMailAsync(msg, cts.Token)`
  （`.WaitAsync` 只是不等了、底層還在跑），前端補 90 秒 AbortController。
  ⚠️ **逾時不可判成失敗** —— 信可能已經送出，判失敗會讓使用者再按一次而對方收到兩封。走「未確認送出」那條路。
- **B**：信箱格式在端點層先驗。⚠️ 驗證器**必須接受沒有點的網域**（`Chih_Kuan_Chang@UMCG`）與 Outlook 的「姓名 <位址>」。
  順便解掉 `a@x.com;b@y.com` 在 smtp 被擋、**在 dbmail 會真的寄給兩個人**。
- ⚠️⚠️ **這批動到了在 IIS 實測過的寄信路徑。** 未做的 D／E／F 見第 3 節與原始清單。

### 第 43 批（2026-09-02）同一階段的未壓日期通知，存檔後只自動問一次
- 使用者：「只要同一個狀態**寄信一次成功**、我就不強迫每次儲存都會跳出視窗詢問」。
- `phaseNotifiedEntry()` 的四條界線：①判定鍵是 **(需求, 階段, 收件者信箱)**；②基準線是同一階段最後一次 `規格回退` 之後
  （**必須按 phase 過濾**）；③依據是**稽核列不是 localStorage**；④**收件者換人就重問**
  （使用者原話：「變更的負責人沒收過信件，完全不知道有這件事。**我一定要盡到有通知的責任**」）。
- ⚠️ **判不出來時一律當成「還沒通知」** —— 多問一次只是吵，**少問一次是下一棒完全不知道有這件事**。`未確認送出` 不算寄過。
- ⚠️ 重複跳窗的代價不是煩，**是把真正該響的那一次一起消音**（手會學會看到就按取消）。
- ⚠️ `fetchHistory()` 必須**回傳剛抓到的那一份**（`setState` 非同步，存檔後那條路不能讀 state）。
- 手動 ✉ **完全不擋**，只多一行「已經在 X 通知過了，這會是第二封」。

### 第 42 批（2026-09-02）新增需求時 `1_EMSEnd` 改為選填
- 必填條件只剩「**原本就有值**」（仍擋得住手動清空既有的結束日）。
- ⚠️ 紅星**不可寫死**（`specEndRequired`）—— 標了紅星卻存得進去、或沒標卻被擋，兩種都比沒有提示更糟。
- ⚠️ 存檔後的自動詢問**只在真的寄得出去時才跳**（使用者：「有信箱時才詢問」）。
  ⚠️ **這不是把失敗吞掉**：那一列的「⚠ 未壓日期」與 `✉` 照樣在，按下去仍會說要補什麼。
  差別在**誰起的頭** —— 他自己按 ✉ 是在問「寄了沒」，非講不可；存完檔是系統插話，講一件他此刻無能為力的事只是噪音。

### 第 41 批（2026-09-01）送信方式可切換（`Mail:Mode` = `smtp` / `dbmail`）
- `dbmail` 借用「DB 主機 → relay」那條已經通的路（網站在 p58esiap12、DB 在 p58esiap08，relay 依來源 IP 白名單放行）。
- ⚠️ **內容、收件者、副本、寄件者規則兩種完全共用**，換的只有最後怎麼送出去；`dbmail` 不需要 `Mail:Host`。
- ⚠️⚠️ **`sp_send_dbmail` 是丟進佇列就回傳** —— 它回的是「已排入」不是「已送出」，失敗會躺在 `sysmail_faileditems`，
  不會回到 API 也不會回到畫面。**一定要輪詢 `sysmail_allitems` 確認 `sent_status`**；確認不到一律回 `queued: true`，
  措辭**不可以是「已寄出」**，稽核列要標「未確認送出」。
- ⚠️ SMTP 那條**刻意保留不刪**（已完整測過，切回去只是改一個字）。

### 第 40 批（2026-09-01）修「點了寄信 icon 沒反應」
- ⚠️ **`SmtpClient.Timeout` 管不到 TCP 連線建立那一段** —— 實測 Timeout 設 8 秒、連一個會丟封包的位址，
  整整 **22 秒**才回來（作業系統的 SYN 重試，設定檔怎麼調都沒用），而預設值是 **100 秒**。
  加上自己做的 TcpClient 探測後同一情境 **8.4 秒**返回。
- ⚠️ 失敗訊息要講「**下一步去查什麼**」（`MailFailureHint()`）：.NET 的例外永遠是「Failure sending mail.」，
  分不出位址錯／防火牆擋／relay 不讓這台轉信，而那三種都不在程式這一側、要找的人還不一樣。
  判斷看 `SocketException`，並把 `Test-NetConnection` 指令連同「**一定要在跑網站的那台主機上跑**」一起印出來。

### 第 39 批（2026-08-31）⚠ 未壓日期 → 一鍵寄信通知下一棒
- `POST /api/requirements/{id}/notify-unset`。收件者＝那一階段的負責人、副本＝另一邊（①④ EMS、②③ MSD）。
- ⚠️ **收件者、階段、主旨、內文一律由後端自己算**（`UnsetPhaseOf()`），前端送什麼都不看 ——
  收件者若能由呼叫端指定，任何網頁都能借系統的名義寄信。
- ⚠️ 順序是**先寄信、再寫稽核列**；稽核列寫失敗**不可以回失敗**（信已送出，使用者會再按一次）。
- ⚠️ **寄件者＝按下按鈕的那個人本人**（Windows 帳號剝網域＝工號 → `dbo.Assignee.EMPO` → `EMAIL`）。
  ⚠️⚠️ **只有 `actorSource == "windows"` 才可以**：模擬帳號走這條路是真的冒名。
- ⚠️ 副本**刻意不因為「等於寄件者」而拿掉**（使用者舉的例子就是宸詳通知明翰、cc 宸詳自己）。
- ⚠️ **`✉` 一定要疊在徽章下一行，不可貼右邊** —— 內嵌實測讓 16 欄的最小寬度多 11px；疊下面**一個 px 都不多**。

### 第 38／37／36／35／34／33 批（2026-08-27，同一天）
- **38**：移除「延期完成 2 次以上」篩選（併回「有延期完成」）。⚠️ 舊網址 `?alert=delay2` 過不了白名單 → 退回預設並從網址清掉。
- **37**：「執行延期」正名為 **`延期完成`**，用語一次對齊五處。**選項名稱講不清楚時，正解是把名稱改對，不是繼續補尾巴。**
- **36**：修工具列破版。⚠️ **原生 `<select>` 的寬度由「最長的那個 option」撐出來，不是由選中的值** ——
  第 34 批加的 22 字說明把「警示」從 140px 撐成 **365px**，把動作鈕擠到第二行。**那是我自己造成的迴歸。**
  ⚠️ 第二個理由：**option 的文字同時是選中後顯示在收合狀態的文字**，補述會被截成半句。
- **35**：軌跡收合（**一次動作＝一張卡**，只併相鄰且五項全同的）＋ 新增 ChangeType `重新排程`。
  ⚠️ 回退剛好會把 End 清成 NULL，於是重新排程被判成 `init`、沉到「初始時程」區。
  ⚠️ `LastChangeTypeByPhaseAsync()` 要**一次查完四個階段**（匯入是逐列呼叫，迴圈裡查會每列多送四趟）並吃同一個 `tx`；
  新增時整個跳過。⚠️ `重新排程` **不進 `isDateChange`**、**不強制理由**、**不可併進 `日期異動`**。
- **34**：「警示」下拉的「延期」補說明（第 36 批證明這個做法錯了，第 37 批改成把名稱改對）。
  ⚠️ 說明要寫進 `<option>` 文字裡不能用 `<option title>`（原生 select 不顯示 option 的 title）。
- **33**：新增「已到階段卻沒壓日期」＝逾期未壓（規則見第 2 節）。
  ⚠️ 當時全表 62 筆一筆都不符合，是造暫時資料驗的；**現在有真實資料（4 筆）可以直接測**。
  ⚠️ 那個組合**用 API 建不出來**（gating 會回 400），要直接改 DB。

### 第 32 批（2026-08-24）投影模式改成有前置條件
- **只有精簡模式 + 需求列表頁才能開**；投影中不給關精簡模式；切到統計報表自動退出；
  載入時若 localStorage 組出 `present && !compact` **直接退出投影**。
- ⚠️ 在此之前是「按下投影就順手幫你打開精簡模式」，那個**借用**製造了兩次「版面跑掉」的回報（第 30、31 批）——
  只要「投影 + 16 欄」在任何一條路徑上組得出來，可用寬度就一定小於 16 欄的需求。
- ⚠️ 淺色底**仍然是借用**（投影機黑階偏灰），離開時還原。

### 第 31 批（2026-08-24）修「UI 在非精簡模式會跑掉」
- 根因與第 30 批同一個：**可用寬度 = 視窗寬 ÷ zoom 倍率**。實測 1440 螢幕 / 16 欄：100% → 0、115% → 30px、130% → **216px**。
- ⚠️ **不要自動幫使用者改設定** —— 放大是他自己按的，靜靜把欄位收起來更難理解。（第 46 批第二段違反過一次，被否決。）

### 第 30 批（2026-08-24）修「投影的情況下版面會跑掉」
- ⚠️ 「借用精簡模式」只寫在 `togglePresent` 裡 —— **投影開著時按 F5，借用不會跑到**，
  畫面就落在「投影 1.5 倍 + 16 欄」這個從沒被設計過的狀態（1440 螢幕 ×150%：可用寬只剩 950px，橫捲 470px）。
- ⚠️ **投影模式下不套 `max-w` 上限**（那時最需要把表格攤開）。
- ⚠️ 量 `scrollWidth - clientWidth` 要**同步量，不可包 rAF**（背景分頁不呼叫，警告會靜靜永遠不出現）。

### 第 29 批（2026-08-24）可及性 + 字級 + 搜尋防抖 + 窄螢幕
- 改之前全檔 `aria-label` / `role` / `tabIndex` 各 **0** 個，**鍵盤完全展不開任何一列**。
- ⚠️ 刻意**不**把 `role="button"` + `tabIndex` 掛在 `<tr>` / `<th>` 上（會讓表格在無障礙樹上失去列／欄結構）。
  可排序表頭走 `sortProps()`；**Space 一定要 `preventDefault`**，否則按下去會順便捲一頁。
- ⚠️ 六個 Modal 共用一份焦點管理。**「開窗前的焦點」不可以在視窗開起來之後才讀 `document.activeElement`** ——
  React 的 `autoFocus` 在 commit 階段就套用了，比 `useEffect` 早，讀到的是視窗裡等一下就被卸載的輸入框，
  還原永遠失敗**而且失敗得很安靜**。改用一直記錄「最後一個不在視窗裡的焦點」（`lastOuterFocusRef` + `focusin`）。
- ⚠️ 也**不要去搶已經 `autoFocus` 的焦點**，沒人接手時聚焦視窗容器本身（`tabIndex={-1}`）就好。
- ⚠️ Esc 的 handler 要放進 ref、listener 只掛一次；**不可以只把相依換成 `!!editingData` 這種布林** ——
  `closeEdit()` 的閉包會停在開窗當下那份 `editingData`，`isEditDirty()` 永遠回 false，Esc 會直接關掉不問。
- ⚠️ 放大一律用 CSS `zoom`，**兩個 zoom class 不可同時掛在同一元素上**（會相乘：1.3×1.5=1.95）。
- ⚠️ 窄螢幕（≤1024）用 `compactPref || narrow` 這種**衍生值**，**不可 `setCompactPref(true)`**（會蓋掉偏好且拉寬後回不去）。
  ⚠️ `matchMedia` 的 `change` **一定要配一個 `resize` 備援**（實測有環境寬度變了、`matches` 也翻了，但 `change` 從沒送出來）。
- ⚠️ 資料列裡任何按鈕都要 `stopPropagation`（外層 `<tr>` 有展開的 onClick，不擋會「展開後立刻被蓋掉」）。
- ⚠️ 每列的編輯／刪除 `aria-label` **一定要帶 NID** —— 64 列念 64 次「刪除」分不出停在哪一筆，而那是刪除鈕。
- ⚠️ 搜尋防抖之後**網址也要吃防抖後的值**（否則每打一個字寫一次網址）。

### 第 28 批（2026-08-24）篩選狀態看得見 + 寫進網址
- 每個生效中的條件都要看得見、可單獨移除。⚠️ 尤其 `colFilters`：**被收起的欄位其篩選值照樣在過濾**，那些晶片要標警示色。
- ⚠️ 網址是 `replaceState` 單向（state → 網址）：**只在載入當下讀一次**（每次 render 都讀會與 state 互相蓋、打字打到一半被回捲）；
  **不可改成 `pushState`**（搜尋框每打一個字就洗掉一個上一頁）；路徑用 `window.location.pathname`（子路徑部署）；
  **認不得的值一律退回預設並從網址上清掉**（否則做出一個永遠 0 筆、又找不到原因的清單）。
- ⚠️ **刻意不在晶片列再放一顆「✕ 清除全部」** —— 工具列那顆就在正上方一張卡的距離，同一個動作不放兩顆。

### 第 27 批（2026-08-24）左側凍結欄 + 重新整理鈕
- ⚠️ 只能凍**連續的前綴欄**；第二欄的 `left` 要**量測**不可寫死 44px（實測 37/42/41px 三種）。
  量測的相依陣列要含 `requirementsData.length` 與 `showColFilters`（`ResizeObserver` 對 `<th>` 不回報寬度變化）。
- ⚠️ **凍結欄的底色必須是「不透明卡片色 + 疊上列底色」** —— 三個列底色有兩個是半透明的，
  而 64 列裡 45 列是 Done，半透明等於沒凍、右邊的欄位會透出來變成兩層字疊在一起。
- ⚠️ 資料列底色與 hover **一律走 CSS**，不可退回 `<tr>` 的 inline style（凍結欄有自己的 background，
  JS 只改 `tr` 會做出「中間亮、左邊兩格沒亮」）。
- ⚠️ `max-w` 必須是**完整字面量**，不可拼成 `max-w-[${w}px]`（Tailwind 掃不到，靜靜不生效）。
- ⚠️ 重新整理**一定要 `fetchReqs` + `fetchHistory` 一起抓**；**不可包進 `runExclusive()`**（那是寫入用的互斥鎖）。

### 第 26 批（2026-08-24）寫入互斥 + 兩條抓取路徑 + 一次算完的驗證
- ⚠️ 每個寫入都要 `runExclusive()` + 按鈕 `disabled`，而且**一定要有 `submittingRef`** ——
  兩次點擊落在同一個 tick 時第二次讀到的 `isSubmitting` 還是舊值，**只靠 state 擋不住真正的連點**
  （實測連按三下「確認新增」會送出 3 個 POST）。
- ⚠️ `fetchReqs()` 分「首次載入」與「重抓」（`loadedOnceRef`）：只有首次才 `setIsLoading(true)`，
  否則每存一次檔 64 列就整片消失再長回來。
- ⚠️ 驗證**一次算完**（`validateEdit()` 回 `{fields, groups}`），不可退回「一段一個 return」
  （缺三個必填要按三次儲存、看三次彈窗，而且關掉後畫面上沒有一格是紅的）。

### 第 21~25 批（2026-08-22~23）五輪邏輯總體檢
> 這五輪的結論全部已寫進 `CLAUDE.md`（寫入端點的三條不變量、`StatusIs()`、查詢端點的 try/catch、
> 樂觀鎖、跨站防護、軟刪除留稽核…）。這裡只留 `CLAUDE.md` 沒寫的判斷理由：

- ⚠️ **樂觀鎖的 token 一定要帶到秒** —— 顯示用的 `updatedAt` 只到分，拿它當 token 等於沒有鎖。
- ⚠️ **`DELETE` 收 body 一定要明寫 `[FromBody(EmptyBodyBehavior = EmptyBodyBehavior.Allow)]`** ——
  Minimal API 只對 `POST`/`PUT`/`PATCH` 推斷 body，寫成推斷會讓**整個 App 一啟動就掛，而 `dotnet build` 不會報錯**。
- ⚠️ **跨站防護只加在 `/api/import`**：其他端點是 `application/json`（會觸發 preflight），
  而匯入收 `multipart/form-data`，那是 CORS 的 **simple request** —— 別的網站放一個 `<form>` 使用者點一下就 TRUNCATE 了。
  `IsCrossSiteRequest()` **只在能明確判斷是跨站時才拒絕**（curl / 測試腳本兩個標頭都不帶所以照常可用）。
- ⚠️ **匯入清空之前的四道前置檢查排在 `BeginTransaction()` 之前**：交易能保證「失敗就回捲」，
  但**回捲不了「成功地匯入了一份錯的檔案」**。在此之前只有一句註解、沒有任何一行真的中止流程。
- ⚠️ **讀取側不可以假設寫入側已經收乾淨**（`StatusIs()` 先 Trim 再比大小寫）：
  `"Done "` 這種舊值會讓前端不給按的需求「直接打 API 卻整個放行」。
- ⚠️ **`dbo.Assignee` 改名／改部門與刪除的後果一字不差** —— 控表存姓名字串、沒有外鍵。
  使用者選定「回 409，改用**停用 + 新建**」。⚠️ **刻意不做連動 `UPDATE dbo.Controltable`**（會靜靜改掉既有需求且沒有稽核列）。
  ⚠️ 只在 `NAME`/`DEPT` **真的被改動時**才驗（否則按「停用」都會被擋）。
- ⚠️ **查詢端點失敗時，前端要看得出「讀取失敗」與「沒有資料」的差別**：
  `/api/history` 掛掉時原本畫面上會變成「**這批需求從來沒被改過**」；`/api/assignees` 掛掉時
  負責人下拉一個名字都沒有、而 EMS 是必填 → 新增根本存不進去，畫面卻只寫「必填欄位未完成」。
  ⚠️ `assigneeList` 失敗時**不清空**（舊名單過期了還能用），`historyEntries` 相反（錯的數字會騙人）。
- ⚠️ 列印時「操作」欄整欄消失，`colSpan` 要跟著少一欄，而且**一定要 `ReactDOM.flushSync`** ——
  `beforeprint` 是同步事件，走一般 `setState` 印出去的還是舊欄數。
- ⚠️ **`AssigneeModal` 要寫成普通函式 `renderAssigneeModal()` 不是 `<AssigneeModal />`** ——
  元件寫法會讓它每次 render 重新掛載，輸入到一半的工號／姓名全部消失。
  ⚠️ **不能只把它改成普通函式**：函式裡不能呼叫 hooks，state 一定要先提上去。
- ⚠️ `13_nid_unique.sql`：重複 NID 被擋的錯誤碼是 **2601**（正是 `IsUniqueViolation()` 捕捉的號碼）。
  ⚠️ **刻意不做啟動時的 bootstrap 建索引** —— 有重複資料時建索引會失敗，那會變成 App 起不來。
- ⚠️ 遷移腳本刻意**不碰**三類無法自動判斷的髒資料 —— 猜錯一次就是整表資料損毀。

### 第 11~20 批（2026-08-18~20）七批修改計畫 + 版面
> 規格語意見 `FIELD_SPEC.md`，流程規則見 `CLAUDE.md`。只留坑：

- **第 13 批（稽核表 + Windows 帳號）**：⚠️ 稽核表比先後**一律用 `Id` 不用 `ChangedAt`**
  （同一秒內的兩筆分不出順序）—— 與後來樂觀鎖 token 要帶秒是同一類坑。
- **第 14 批（gating）**：⚠️ 只擋「從空白開始填寫」（見第 2 節第 2 個關節）。
- **第 15 批（Done 推進 / 提早 vs 延期）**：⚠️ 延期時 **End 不動、只寫 `ActualEnd`**。
  ⚠️ **刻意不做成完全鎖死** —— 匯入資料階段填錯一定會發生。
- **第 16 批（規格回退）** / **第 17 批（警示徽章）**：⚠️ 三個計數欄是稽核表的快取（第 4 個關節）。
- **第 12 批（明細表整併）**：到期預警頁籤在此移除 —— **不再維護第二套格式**。
  ⚠️ 「Done 置底」開著時結案的案件仍會被排到下方（延期最多的往往正好是結案的）。
- **第 11 批（RegDate）**：⚠️ `Main Cat` / `Sub Cat` 是僅有的兩個無固定寬度欄位，
  `nowrap` 會讓 td 的 min-content 等於整串文字寬（一筆 Sub Cat 就吃掉 285px）—— 內層 div 要給 `maxWidth`。
- **投影模式（2026-08-19）**：⚠️ `zoom` 只掛 `header` 與 `main`，**不可掛最外層那個 `min-h-screen`**。
- **統計報表（2026-08-20）**：⚠️ 分組年月刻意與趨勢圖共用同一個 `yearMonth`（註冊年月）與同一組區間 state。
  ⚠️ 區間選擇器同樣寫成普通函式 `renderYmRange()`（同 `AssigneeModal` 的理由）。
- **色階（2026-08-19 兩批）**：把文字與框線**加深**是為了投影。⚠️ **沒有調淡任何顏色，也不要「順手調回來」**；
  深色的 tertiary 用 `#b6c2d2` 這個**非 Tailwind 色階是刻意的**。
- **展開指示（2026-08-22）**：`No` 欄流水號前加 ▸／▾。⚠️ **旋轉要掛在外層 `<span>` 不是 `<svg>`**。
- **版面（2026-08-20）**：⚠️ 套了 `.ctl` 這類 class 就**不要再寫 inline 的 background/border/color**
  （inline 會把 `.ctl-on` 整個蓋掉）。⚠️ `<select>` 要補 `select.ctl{display:inline-block}`（替換元素套 `inline-flex` 無效）。
- ⚠️ **JSX 裡寫 `**粗體**` 不會變粗體**，會原樣顯示星號。

### 第 1~10 批（2026-08-16~18）地基
- ⚠️ **Tailwind 一律不可字串拼接 class 名稱**（`bg-${clr}-500/10` 靜態掃描看不到 → 完全不生成，**靜靜不生效**）。
- ⚠️ **CSS 值也不可字串拼接**：`${color}1a` 遇到 CSS 變數會拼成 `var(--x)1a` 這種無效值，底色靜靜變透明。
- ⚠️ **深色模式一定要宣告 `color-scheme`**（`.dark` 掛在 `document.body` 上，會繼承下去所以有效）——
  沒宣告時 `select` 的 option 變白底淺灰字、看起來像被停用。
- ⚠️ **sticky 的儲存格必須是實心底色**（`rgba(...,0.04)` 的 tint 會讓資料列透出來）。
- ⚠️ **Done 列不可用 `opacity:0.5`**（會連文字一起變淡），改用 `--bg-row-done` 淡底色。
- ⚠️ **逾期判定不能只看「日期 < 今天」** —— 第一版這樣寫，7 列有 5 列全紅（2025 年交的 Spec 被標成逾期 334 天），
  反而蓋掉真正落後的那一筆。這是後來 `isPhasePassed()` 的起點。
- ⚠️ **「時程異動」KPI 一度在加總字串長度**（`history.length` 是字元數不是筆數，顯示 236 而實際只有 3 筆）。
- ⚠️ `analytics` 的 `useMemo` 相依陣列曾經是空的 → **戰情總覽永遠顯示 MOCK_DATA 的假數字**。
  同批移除所有假資料：API 掛掉一律顯示錯誤狀態，**不做無聲降級**。
- ⚠️ `toggleRow` 不可用 `item.nid` 當 key（NID 手動輸入會重複）→ 一律用 `item.id`。
- ⚠️ 軌跡的 History 欄只存「原日期」，**新日期是從下一筆的原日期推回來的**，最後一筆對應目前的實際值（`buildTimeline()`）。
- 匯入欄位對應：**先做「完全相符」比對，剩下未認領的表頭才做「包含」比對** —— 否則 `MSD` 會誤命中
  「(2)評估日期 (MSD 填寫) Spec Confirm」。
- ⚠️ `FormatYearMonth()` 要吃 `26/Dec` 與 `Jul/26` **兩種順序**，認不出來的**原樣留著不猜**。

---

## 8. 使用者的溝通偏好

- **檢查邏輯／體檢類的工作：先分級出清單，讓他挑做哪幾組**，不要直接開始改。
  **沒挑中的要留證據**（就是第 3 節與第 4 節）。
- 回報 UI 問題時他常附截圖，而且用詞很直接（「變成兩行了!!」「看起來像是網頁哪邊壞了」）——
  那通常代表**畫面上有一個他當下修不了的東西**，先量再答。
- 改到**畫面上看得見的行為**就要一併更新 `docs/使用者手冊.html`（判斷標準是「畫面上會不會不一樣」，
  純內部重構不算）。手冊抬頭那個「版本」要手動對齊當次的 `?v=`。
