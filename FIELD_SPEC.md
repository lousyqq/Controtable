# FIELD_SPEC.md — 欄位定義與業務規則（使用者定義，權威來源）

來源：使用者於 2026-08-17 提供的完整欄位定義。
**本文件是欄位語意與 UI 行為的權威來源**；`DB_table.md` 只描述資料庫實體結構，兩者衝突時以本文件為準（並以累加腳本調整 DB）。

---

## 22 個欄位對照表

| # | Excel 欄名 | DB 欄位 | Web 顯示 | 說明 |
|---|---|---|---|---|
| 1 | `NID` | `NID` | NID | 需求流水編號，**唯一值不可重複**。從 1 遞增。目前人工輸入；新增時若重複要**跳視窗提示** |
| 2 | `OverallStatus` | `Status` | Status | 整體狀態：`Init`（尚未開始）／`Ongoing`（執行中）／`Pending`（暫緩）／`Done`（結案） |
| 3 | `StatusID` | `StageCode` | StatusID | 狀態編號 **`1`~`5`（純數字，無括號）**：1.EMS規格確認、2.MSD確認中、3.MSD開發中、4.EMS驗收、5.結案。**資料列上顯示「代號 + 階段名稱」的彩色 pill**，不是只有數字 |
| 4 | `RegDate` | `RegDate` | 註冊日期 | 需求的註冊日期，**一律 `YYYY/MM/DD`**（DB 為 `DATE`，API 以 `YYYY-MM-DD` 傳遞）。新增時預設為當天，畫面上唯讀 |
| 4b | `YearMonth` | `YearMonth` | *(不顯示)* | 年月 `YYYY/MM`。**已降級為 `RegDate` 的衍生值**，寫入時一律由 `RegDate` 反推，只保留給 Excel 匯入匯出與總覽趨勢圖分組。匯入舊格式（只有年月、沒有日）時 `RegDate` 補該月 **01 日** |
| 5 | `MainCat` | `MainCat` | 專案名稱 | 專案項目名稱 |
| 6 | `SubCat` | `SubCat` | 子項目分類 | 專案項目中的子分類 |
| 7 | `Remark` | `NotesLink` | 需求補充 | 針對子分類的描述補充。
| 8 | `EMS` | `EmsOwner` | EMS | 負責提需求 Spec 的 EMS 部門 Owner |
| 9 | `1_EMSStart` | `SpecStart` | *(detail)* | EMS 提 Spec 開始日 `YYYY/MM/DD` |
| 10 | `1_EMSEnd` | `SpecEnd` | EMS 提Spec | EMS 提 Spec 結束日 `YYYY/MM/DD` |
| 11 | `1_EMSHistory` | `SpecHistory` | *(detail)* | ① 階段的變更歷史 |
| 12 | `MSD` | `MsdOwner` | MSD | 收需求後負責開發的 MSD 部門 Owner |
| 13 | `2_MSDConfirm` | `MsdConfirm` | MSD確認需求 | MSD Owner 確認 EMS Spec OK 的日期 `YYYY/MM/DD` |
| 14 | `2_MSDHistory` | `MsdConfirmHistory` | *(detail)* | ② 階段的變更歷史 |
| 15 | `3_MSDStart` | `MsdStart` | *(detail)* | MSD 開發開始日 `YYYY/MM/DD` |
| 16 | `3_MSDEnd` | `MsdEnd` | MSD 開發 | MSD 開發完成日 `YYYY/MM/DD` |
| 17 | `3_MSDHistory` | `MsdHistory` | *(detail)* | ③ 階段的變更歷史 |
| 18 | `4_EMSStart` | `UatStart` | *(detail)* | EMS 開始驗收日 `YYYY/MM/DD` |
| 19 | `4_EMSEnd` | `UatEnd` | EMS 驗收 | EMS 驗收完畢日 `YYYY/MM/DD` |
| 20 | `4_EMSHistory` | `UatHistory` | *(detail)* | ④ 階段的變更歷史 |
| 21 | `StatusDesc` | `CurrentStatus` | *(detail)* | 需求目前的現況描述 |
| 22 | `MP Saving` | `MpSaving` | MP Saving | 完成後可提供的 Benefit，例：`0.5 人/月` |
| 23 |`NotesLink` | 超連結 |

*(detail)* = 預設不出現在資料列上，只在點開的明細區顯示。

---

## 專案執行期間（四階段）

編輯視窗分成四個獨立區塊，各自有自己的解鎖鎖頭、異動理由欄與異動軌跡：

| # | 區塊標題 | 日期欄位 | 軌跡寫入 |
|---|---|---|---|
| 1 | `1_EMS規格確認` | `1_EMSStart` ~ `1_EMSEnd` | `1_EMSHistory` (`SpecHistory`) |
| 2 | `2_MSD確認中` | `2_MSDConfirm`（單一日期） | `2_MSDHistory` (`MsdConfirmHistory`) |
| 3 | `3_MSD開發中` | `3_MSDStart` ~ `3_MSDEnd` | `3_MSDHistory` (`MsdHistory`) |
| 4 | `4_EMS驗收` | `4_EMSStart` ~ `4_EMSEnd` | `4_EMSHistory` (`UatHistory`) |

> ⚠️ **改的是「顯示名稱」，Excel 欄名與 DB 欄名一律不動**。
> `1_EMSStart` / `2_MSDHistory` 這些是匯入對應表 (`Program.cs` 的 `fieldCandidates`) 的 key，
> 改了匯入匯出就對不上。

> ⚠️ **② 與 ③ 的日期都掛在 API 的 `msd` 物件下**（`msd.confirm` vs `msd.start`/`msd.end`），
> 但是**兩個獨立階段**：各自解鎖、各自要理由、軌跡寫到不同欄位。
> 前端用 `PHASES` 設定表（`app.jsx`）驅動，不可再用階段代號直接當物件名索引。

### 階段順序 gating（第 14 批）

前置階段的日期填完，下一階段才開放「**從空白開始填寫**」：

| 階段 | 開放條件 |
|---|---|
| `1_EMS規格確認` | 永遠開放 |
| `2_MSD確認中` | ① 的 `Start` 與 `End` **都**已填 |
| `3_MSD開發中` | ② 的 `Confirm` 已填 |
| `4_EMS驗收` | ③ 的 `Start` 與 `End` **都**已填 |

> ⚠️ **gating 只擋「從空白開始填寫」**。已經有值的欄位一律照舊（解鎖後可改）。
> 現有資料有階段跳空的（③ 有日期但 ② 空），若寫成「前置沒填就整個 disable」，
> 那些列會**有值卻永遠改不動**。① 永遠開放，所以一定能從前面補回來。

- 判定看的是**編輯視窗當下的值**，不是存檔後的值 —— 在同一個視窗裡把 ① 補完，
  ② 立刻開放，不必先存檔再重開。
- 只比對**直接前置**。前置自己沒開放時它也還是空的，整條鏈自然逐層關著；
  往上遞迴反而會把跳空資料的下一階段一起鎖死。
- **兩種鎖語意不同，icon 與顏色刻意分開**：
  - 🔒 **灰色實心**（`GateLock`）= 前置未完成，**不可解**，補完前置就自動開放
  - 🔓 **標題旁的線條鎖** = 已有值防誤改，**點一下可解**
- 後端 `PhaseGatingViolations(req, before)` 在 POST / PUT 都擋，回 `400`。
  判定與前端一致：**只看「本來是空的、這次被填進去」的欄位**（新增時 `before` 視為全空）。

### 階段完成 Done（第 15 批）

每個階段標題旁有一顆「✓ 完成」鈕（**已壓了 End 日期才會出現**）。按下去時以「今天」比對原訂 End：

| 情況 | End 欄位 | 實際完成日 | 計數 |
|---|---|---|---|
| 今天 **≤** 原訂 End → **提早完成** | **更新為今天** | 不寫 | `EarlyCount + 1` |
| 今天 **>** 原訂 End → **延期完成** | **保持不變** | 寫入今天 | `DelayCount + 1` |

- ② `2_MSD確認中` 只有單一日期，它的「End」就是 `2_MSDConfirm`。
- **`StatusID` 自動推進**：①→2 ②→3 ③→4 ④→5，**只前進不後退**
  （在 ④ 的案子回頭補按 ① 的完成，不該被拉回 2）。
- **`OverallStatus` 連動**：推到 `5` 時自動 `Done`；離開第 1 階段時 `Init` → `Ongoing`。
  `Pending` 是人工壓的，不會被自動蓋掉。
- 兩種都會寫一筆稽核列（`提早完成` / `延期完成`），說明欄記「提早/延期 N 天」。
- **同一階段不可重複標記完成**，否則計數會變成假數字；後端查稽核表擋下回 `409`。
  只看「最後一次規格回退之後」的紀錄 —— 回退後那個階段本來就要重做。
- 視窗裡改了日期還沒儲存就按完成會被擋下（會拿舊值比對，結果與畫面對不起來）。

> ⚠️ **延期完成時資料列一定要顯示「原訂 → 實際」**。End 刻意保持不變，
> 只顯示原訂日期的話主管永遠看不到落差。資料列的呈現是原訂灰字、下一行 `→ 實際` 紅字；
> 明細與軌跡同理，而且**延期的原訂日期不可畫刪除線** —— 它沒有被改掉。

### 規格回退（第 16 批）

規格變更導致前面的階段要重做時，從編輯視窗 StatusID 下方的「🔄 規格回退」進入
（**`StatusID` ≥ 2 才會出現**，第 1 階段沒有更早的可退）。

- 可退回**先前任一** `StatusID`（1 ~ 目前-1）。
- **清空範圍：≥ 目標階段的全部日期，含目標階段本身**。
  例：目前在 ④、回退到 ② → 清空 ②③④ 的日期。
  回退到 ② 的語意就是「這個確認要重做」，所以 ② 自己的日期也清掉。
  四個 `*ActualEnd` 跟著清；`MsdConfirmNote` 是自由文字不是日期，**不清**。
- **三個計數欄不清**（`DelayCount` / `EarlyCount` / `RollbackCount`）——
  那是既成事實，主管要看的就是這個。回退本身讓 `RollbackCount + 1`。
- **文字說明必填**；異動原因分類**固定記為「規格變更」**，不讓使用者選。
- **清空前先把完整快照寫進稽核表**（每個被清的階段一列，`ChangeType='規格回退'`），
  說明欄記成「由 X 回退至 Y：<使用者輸入>」。清掉就拿不回來了，順序不可反。
- `OverallStatus` 若是 `Done` 會回到 `Ongoing`（否則會出現「已結案但 StatusID=2」的矛盾）。
- 回退後那些階段回到「未填寫」，**後面的階段自動重新鎖定** ——
  這是階段順序 gating 的自然結果，沒有另外寫程式。
- 回退後該階段**可以重新標記完成**（Done 的重複檢查只看最後一次回退之後的紀錄）。

> ⚠️ 回退到 `1` 會把 `1_EMSStart` / `1_EMSEnd` 也清掉，而這兩欄是**必填**，
> 清空後必須重新填寫才能儲存這筆需求。視窗上會明確提示這件事。

### 警示徽章（第 17 批）

資料列的 **NID 欄下方**顯示兩個**互不影響**的標籤（`DelayCount` / `RollbackCount` 為 0 就不顯示）：

| 徽章 | 意義 | 顏色 |
|---|---|---|
| `🔄N` | 規格變更回退 N 次 | 紫（`#8b5cf6`，與規格回退一致） |
| `⏰N` | 執行延期 N 次 | **N ≥ 2 轉紅色警示**；N = 1 用中性色 |

- **兩者刻意分開**，不合併成一個「異常 N 次」—— 回退是規格一直變、延期是執行落後，
  主管要能分開判斷責任歸屬。
- **提早完成不做徽章**（那不是警示），但明細的軌跡本來就查得到。
- **1 次不轉紅**：一次就紅的話整片都是紅字，真正嚴重的反而被淹掉。
- 工具列的「警示」下拉可篩 `有執行延期` / `延期 2 次以上` / `有規格回退`；
  「延期最多」「回退最多」兩個排序開關依次數由多到少排。
  ⚠️ 「Done 置底」開著時結案的案件仍會被排到下方，tooltip 有寫明。
- **直接讀 `DelayCount` / `RollbackCount` 欄位，不 parse 稽核表** ——
  要能排序與篩選（例如「延期最多的前 5 筆」），每列都掃一次稽核表撐不住。
- ⚠️ **版面**：徽章掛在 NID 欄下方而**不是新增欄位**。資料列已經很擠
  （全表 min-content 約 1259px），實測加了徽章之後表寬沒有變化。

### 各區塊的共同規則

- **異動理由只在「日期真的被改掉」時才強制**。解鎖了但沒動日期 → 不需要理由、也不會產生軌跡。
  首次填寫（原本是空的）同樣不需要理由。
- **`End Date` 不可早於 `Start Date`**（同一天可以）。三個有區間的階段（①③④）前後端都擋，
  `<input type="date">` 另外加了 `min` / `max` 讓日期選擇器直接反灰。
  ② 只有單一日期，不受此限。
- **② MSD 確認Spec 沒有備註輸入欄**。DB 的 `MsdConfirmNote` 保留，既有資料
  （例如 `Next Check: 8/18 -> 8/20`）仍會顯示在展開的明細裡，但不再提供編輯。

### 到期預警：StatusID ↔ 該盯的日期

每週會議要 review 快到期的需求。判定規則是**先用 `StatusID` 定位目前卡在哪一階段，
再只比那一個日期**，不是四個日期一起比：

| StatusID | 目前階段 | 比對的日期 | 交件方 |
|---|---|---|---|
| `1` | ① EMS規格確認 | `1_EMSEnd` (`SpecEnd`) | EMS |
| `2` | ② MSD確認中 | `2_MSDConfirm` (`MsdConfirm`) | MSD |
| `3` | ③ MSD開發中 | `3_MSDEnd` (`MsdEnd`) | MSD |
| `4` | ④ EMS驗收 | `4_EMSEnd` (`UatEnd`) | EMS |
| `5` | 結案 | — 不預警 | — |

- `OverallStatus = Done` 一律不預警。
- **回退規則**：`StatusID` 沒填、超出 1~5、或該階段的日期還沒壓時，
  改取「**最後一個已經壓了日期的階段**」——後面的階段既然還沒排程，現在該盯的就是這一個。
  這種推斷出來的階段在畫面上會標「(推斷)」。
- 天數視窗（預設 **7 日**）只限制「尚未到期」的項目；**已逾期的一律列入**。

> ⚠️ 不可改回「四個日期一起比」。早就走完的階段（例如去年交的 Spec）會永遠亮紅燈，
> 把真正該關注的項目淹掉——這個坑在 2026-08-16 已經踩過一次。

### 壓日期的兩種情況

| 情況 | 呈現 |
|---|---|
| 直接填寫日期 `YYYY/MM/DD` | 一般樣式 |
| 壓 `Next check: YYYY/MM/DD` | **日期標示為藍色**，說明文字顯示在資料列上方 |

---

## Web 資料列預設欄位（由左至右，14 欄）

```
No │ NID │ Status │ StatusID │ 註冊日期 │ 專案名稱 │ 子項目分類 │ EMS │ MSD ║
1_EMS規格確認 │ 2_MSD確認中 │ 3_MSD開發中 │ 4_EMS驗收 │ MP Saving
```

實作上另有兩欄功能性欄位（`Notes Link` 圖示／`操作` 按鈕），故 DOM 上共 16 欄。

- **`Notes Link` 欄排在 `子項目分類` 右邊**（2026-08-19 使用者要求），仍屬「專案基本資訊」
  群組（群組 `colSpan` 不變，仍是 8），並接手該組最右側的 2px 分隔線；精簡模式收起
  `Notes Link` 時，2px 分隔線還給 `子項目分類`。
- **進階篩選的漏斗圖示放在最右邊 `操作` 欄的欄名格**（2026-08-19，原本掛在 `Notes Link`
  表頭上）。工具列搜尋框旁那顆漏斗是同一個開關。

- **`No` 是畫面流水號**（2026-08-19 加入，兩種模式都有）：目前排序與篩選下的第幾列，
  **不是 `NID`、也不是 DB 的 `Id`**，篩選後會從 1 重新編。刻意不可排序 —— 依它排序等於
  依「第幾列」排序，沒有意義。整列的到期風險色條也掛在這一欄（它永遠是第一欄）。
- **欄位名稱一律單行不斷行**（2026-08-19 使用者要求）。四個階段的表頭曾為了省寬度排成兩行，
  改回單行後全表 min-content 從 1214px 變 1232px（實測），1280px 視窗仍放得下。

- **2026-08-19 起，`EMS` 與 `MSD` 兩個負責人欄相鄰**（`║` 處以 2px 粗線分隔人員區與時程區）。
  舊版是 `EMS │ 1_EMS │ MSD │ 2_MSD │ …` 的交錯排法，人員欄插在階段中間，
  橫向掃四個階段的時程時會一直被人名打斷。**不要改回交錯。**
- `專案名稱` 與 `子項目分類` 是僅有的兩個無固定寬度欄位。**2026-08-19 起改為換行完整顯示**
  （`break-words` + `overflowWrap:anywhere`），欄寬固定寫在 `<th>` 上（150px / 190px）。
  不可改回 `truncate`：它的 `white-space:nowrap` 會讓該欄的 min-content 等於整串文字寬度，
  一筆長字串就能把整張表撐開（實測有一筆 Sub Cat 吃掉 285px）；而且截斷後主管要逐筆 hover
  才看得出是哪一筆。換行版的 min-content 只有一個詞寬，配上固定欄寬就穩定了。
- **2026-08-19 起資料表改為整頁捲動**，不再包在 `overflow:auto` + `maxHeight` 的內捲容器裡，
  62 筆一次全部攤開。連帶：表格卡片不可有任何 `overflow`（會變成新的捲動容器，讓表頭 sticky 失效）；
  橫向溢位由頁面本身承接，故 `body` 不可加 `overflow-x:hidden`。
- 兩層表頭的 sticky `top` **必須實際量測**（`app.jsx` 的 `headOffsets`，經 CSS 變數
  `--head-top-group` / `--head-top-col` 傳入），不可寫死。`height` 用在表格儲存格上只是
  「最小高度」，欄名兩行後真實列高會超過寫死值，中間就漏出正在捲動的資料列。
- 全表 min-content 約 **1214px**：1366px 以上完全不需捲動。更窄的視窗可開「精簡模式」。

### 精簡模式（主管檢視，2026-08-19）

```
No │ NID │ 專案名稱 │ 子項目分類 ║ EMS │ MSD │ StatusID ║ 目前階段時程 │ 現況描述   （9 欄）
```

- 收起 `Notes Link` / `Status` / `註冊日期` / `MP Saving` / `操作` 五欄。
- **`StatusID` 移到 `MSD` 右邊**（2026-08-19）：「誰負責 → 卡在哪一階段 → 那一階段何時到期」
  連成一句話，而那個日期本來就是由 `StatusID` 決定的，兩欄相鄰才讀得出因果。
- **`現況描述` 放在最後一欄**（2026-08-19 使用者要求，一般模式仍不顯示）。
  ⚠️ **不可用 `truncate`**：內容常是多行長文，`nowrap` 會讓這一欄的 min-content
  等於整串文字寬度，一筆就把整張表撐爆。照 `專案名稱`／`子項目分類` 的做法
  `whitespace-pre-wrap` + `break-words` + 固定欄寬（260px）完整顯示。
- **四個階段的日期欄併成一欄「目前階段時程」**：只顯示 `StatusID` 對應的那一個日期。
  哪一個階段由 `resolveDuePhase()` 決定 —— 與到期預警、逾期篩選、統計報表「需關注」KPI
  **同一套規則**，所以這欄的紅字必然對得上那些數字。另外兩種情況：
  - 已結案 → 顯示最後一個排定的階段（通常是 `④ EMS驗收`），標「已結案 · 階段名」。
  - `StatusID` 未填／該階段還沒壓日期 → 取最後一個已排定的階段，標「推斷 · 階段名」。
  - 四個階段都沒壓日期 → 顯示「未排定」。
- 7 日內到期／逾期照舊上色；**7 日以外改用灰字顯示「剩 N 天」**（顏色只留給異常）。
- **排序預設為「目前階段到期日近的排上面」**（＝排序面板的「逾期優先」，同一個 state）。
  切進精簡模式時自動打開，可由表頭或排序面板關掉。切出精簡模式時**不會**自動關掉 ——
  使用者在一般模式自己開的排序不該被這顆開關收走。
- 完整的四階段時程仍在**展開明細**裡，資訊沒有消失，只是不佔主管的視線。

## 統計報表

由上而下：KPI 卡 → 風險預警 → **各年月 × 目前階段案件數** → 人員負載 → 各年月案件數。

**2026-08-20 起人員負載與各年月案件數各佔一整列**（原本是 `lg:grid-cols-2` 並排）。
並排時兩張卡各只有半個版面，趨勢圖的柱子被壓得很窄、人員負載的長條短到看不出差距，
投影時尤其明顯。

### 各年月 × 目前階段案件數（2026-08-20）

列 ＝ `StatusID` 五階段，欄 ＝ 年月，右側帶「合計」欄，底部帶「合計」列。

- **分組的年月是 `YearMonth`（＝由 `RegDate` 反推的註冊年月），與下方趨勢圖完全相同**，
  區間也是同一組 state。所以**合計列的每一格必然等於下方那根柱子的總高**。
  ⚠️ 不要改成「目前階段的到期日」分組 —— 看起來更貼近進度，但兩張圖的欄合計就會不一致，
  同一頁上出現兩個對不起來的數字，主管會連整頁一起不信任（第 12 批的教訓）。
- 空 `StageCode` 沿用資料列 B4 的推斷（`Done` 視為 `5`）；仍推不出來的歸「— 未分類」列，
  **不靜靜吃掉** —— 吃掉會讓合計少幾筆而看不出原因。這一列只有真的存在時才出現。
- **0 一律留白不寫 `0`**：有值的格子才跳得出來，滿版的 0 會把真正的數字淹掉。
- 1~5 五列固定都出現（0 件也要看得到「這一階段是空的」）。
- 全部年月攤開時橫向捲動留在卡片內，不可讓它變成整頁的橫向捲動。

### 年月區間（2026-08-20）

**交叉表與趨勢圖共用同一組區間 state**（`ymRange`），控制項只放在交叉表的標題列，
趨勢圖那邊只註明「區間與上方統計表連動」—— 同一頁擺兩組一樣的控制項只會讓人
懷疑它們各管各的。

- 兩個下拉（起／迄）＋ 三顆預設鈕（近 6 月／近 12 月／全部 N）。
  下拉負責「對齊某一季／某一年」，預設鈕負責「快速看最近 N 個月」，寫進同一組 state。
- `{from:'', to:''}` ＝ 自動，取最近 **12** 個**有資料的年月**（不是日曆月，資料本來就會斷月）。
- **起訖被選反了會把另一端一起帶過去**，不會讓畫面直接空掉而看不出原因。
- 預設鈕的選中狀態只在「結尾貼齊最新年月」時才成立 —— 使用者自己挑的區間不該被標成預設。
- `YearMonth` 格式固定 `YYYY/MM`，所以字串比大小就等於時間比大小，不需要 parse 成日期。

### 趨勢圖的讀數（2026-08-20）

**數字一律直接標在柱子上**：總數在柱子正上方，進行中／已完成分別印在自己的色塊中央。
這張圖會投在會議室的牆上，**台下沒有滑鼠可以 hover**。

- 原本的 hover 變暗 + 圖例列讀數已整組移除（連同 `trendHover` state）——
  數字既然常駐，那套互動就只是多一份會壞的狀態。
- 分段數字只有在該段高度 **≥ 16px** 時才印，否則會凸出色塊外面。
- 底部只留圖例與「區間 X – Y．共 N 件」：切了區間要看得出現在看的是哪一段。

### 控制項與版面規格（2026-08-20 版面優化）

純視覺調整，**沒有動任何欄位語意、資料流或篩選邏輯**。目的是收掉「每顆按鈕各自寫尺寸」
累積出來的參差感 —— 顏色與對比一律沿用既有規則，沒有任何一處被調淡。

| 項目 | 規格 |
|---|---|
| 工具列控制項 | 一律 `.ctl`：**高 34px**、圓角 8px、11px 粗體。純圖示版 `.ctl-icon` 為 34×34 方形 |
| 頁首控制項 | 一律 `.ctl-sm`：**高 28px**、圓角 8px |
| 選中態 | `.ctl-on`（靛色實心）。語意色（需關注紅、清除全部紅、階段色）仍走 inline style |
| 卡片 | `.t-card` 圓角 6px → **10px**，陰影加一層 `0 4px 12px -6px` |
| 表頭 | 兩層 `<th>` 上下留白 10px → **11px**（寫在 `.sticky-table thead th`，特異性高於 Tailwind 的 `py-2.5`，不必改 19 個 `th`） |
| 群組分隔 | `.ctl-div`（1px × 20px，色 `--border-soft`）。分隔的是**控制項群組**不是資料，所以刻意比 `--border-card` 淡一階 |

- **頁籤改為分段控制列 `.seg`**：`需求列表` / `統計報表` 是平行關係，
  原本「選中的那顆填滿靛色」看起來像一顆主要動作鈕旁邊擺了一段灰字。
  選中格用卡片色浮起 + `--seg-on-text`（淺色 `#4338ca` 8.4:1／深色 `#a5b4fc` 7.1:1）。
- **資料列的 `Status` 欄從藥丸改成「色點 + 文字」**：同一列右邊還有 `StatusID` 的藥丸，
  兩顆框並排 × 62 列會讓整張表看起來全是方塊。文字改吃 `--text-secondary`（10.4:1），
  **對比反而優於**原本的 `#3b82f6` 疊在 8% 淡藍底上（約 3.6:1）；顏色資訊留在色點上沒有消失。
- **`STATUSES` 的 `lightBg` / `darkBg` / `border` 三個值目前沒有使用者**（只剩 `color` 與 `label`）。
  保留不刪，日後要恢復藥丸樣式時直接可用。
- ⚠️ **套了 `.ctl` / `.seg-item` 就不可以再寫 inline 的 `background` / `border` / `color`** ——
  inline style 特異性最高，會把 `.ctl-on` 的選中樣式整個蓋掉（與 `.icon-btn` 是同一個坑）。
- ⚠️ **`<select>` 必須額外套 `select.ctl { display:inline-block }`**：它是替換元素，
  `inline-flex` 在部分瀏覽器會讓選項文字錯位。

### 文字色階（2026-08-19 整體加深）

**一般模式**也可能直接投在投影機上（不是每個人都會先按投影模式），所以基礎色盤本身加深過一階。
四級文字色與實測對比（對卡片底 `--bg-card`）：

| 變數 | 淺色（改前 → 改後） | 深色（改前 → 改後） |
|---|---|---|
| `--text-primary` | `#0f172a` 17.9:1（不變） | `#f1f5f9` 13.4:1（不變） |
| `--text-secondary` | `#334155` 10.4:1（不變） | `#cbd5e1` 9.9:1（不變） |
| `--text-tertiary` | `#64748b` 4.8 → **`#475569` 7.6** | `#94a3b8` 5.7 → **`#b6c2d2` 8.1** |
| `--text-muted` | `#94a3b8` **2.6** → **`#64748b` 4.8** | `#475569` **1.9** → **`#94a3b8` 5.7** |

- 改前兩個 muted 值（2.6:1 / 1.9:1）**連 WCAG 大字的 3:1 都不到**，
  深色的 1.9:1 在螢幕上就已經是勉強猜得出來，投出去等於整行消失。
  它們用在圖例、「顯示 N / M 筆」這類說明文字上 —— 正是投影時最先看不見的東西。
- ⚠️ **不要為了「更精緻」把 muted 調回 `#94a3b8` / `#475569`**。
- ⚠️ 深色的 tertiary 用 `#b6c2d2` 這個非 Tailwind 色階是刻意的：
  直接用 slate-300 `#cbd5e1` 會跟 `--text-secondary` 完全撞色，四級塌成三級。
- 兩者是**疊加**的：一般模式已經投得出來，投影模式在這之上再提一階（見下）。
- `--text-tertiary` / `--text-muted` 全專案**只當文字色用**（各 40 / 67 處，
  沒有任何 background / borderColor 用法），所以調色不會波及底色或框線。

#### 視窗裡的圖示鈕 `.icon-btn`

編輯視窗與人員視窗的「關閉 ✕」與四個階段的「解鎖鎖頭」原本寫死 Tailwind 的
`text-gray-400` (#9ca3af)：白底只有 2.9:1，而且**不跟著深淺色走**。
改為 `.icon-btn`（`input.css`），吃 `--text-tertiary`，深淺色與投影模式一起套用。

- ⚠️ **用 class 不用 inline style**：inline style 的特異性會把 `hover:text-amber-500`
  這類 hover 色整個蓋掉，鎖頭在按下去之前就沒有顏色回饋。
- `.icon-btn:hover`（primary）與 Tailwind 的 `hover:text-*` 特異性相同，
  靠 utilities layer 排在 components 之後取勝 —— 實測產出的 `app.css` 裡
  `.icon-btn:hover` 在第 47 條、`.hover\:text-amber-500:hover` 在第 322 條，
  所以四個鎖頭走自己的階段色（琥珀／紫／藍／粉），沒指定的 ✕ 落到 primary。
- 同一批順手收掉的還有：人員視窗空狀態的 `text-gray-500`、
  編輯視窗唯讀「註冊日期」輸入框的 `text-slate-500 dark:text-slate-400`
  （改吃 `--text-secondary` —— 它顯示的是真實資料值，要讀得到）。

#### 框線（同批加深）

| 變數 | 淺色（改前 → 改後） | 深色（改前 → 改後） |
|---|---|---|
| `--border-table`（1px 欄列分隔） | `#f1f5f9` **1.06** → `#cbd5e1` **1.48** | `rgba(30,41,59,.5)` **1.00** → `rgba(148,163,184,.25)` **1.58** |
| `--border-card`（卡片外框、2px 群組分隔） | `#e2e8f0` **1.24** → `#94a3b8` **2.56** | `rgba(255,255,255,.06)` → `rgba(148,163,184,.4)` **2.09** |

- 淺色改前是 **1.06:1**（幾乎等於白色），投出去整片表沒有格線，62 列橫著讀一定跳行。
- 深色改前更嚴重：`rgba(30,41,59,0.5)` **就是卡片底色自己**，疊上去 **1.00:1** ——
  那不是「淡」，是根本沒有畫格線。
- **兩個變數刻意用不同色階**：1px 與 2px 的寬度差在投影機上不夠明顯，
  要靠顏色才分得出「欄位分隔」與「群組分隔」。
- 投影模式的框線覆寫值也跟著往下走一階（`#94a3b8` / `#64748b`、
  深色 `.45` / `.65`）—— 否則它會比新基準還淡，投影模式反而變糊。

### 投影模式（會議室簡報，2026-08-19）

標題列的 **`📽 投影`** 開關。**刻意不是第三套版面** —— 它只把既有畫面調到會議室看得見的程度：

| 做法 | 為什麼 |
|---|---|
| `header` / `main` 套 CSS `zoom`（預設 **150%**，可調 125／140／150／175／200%） | 全表 92% 的文字是 10~12px，投出去後排讀不到。`zoom` 會連同 padding、欄寬、sticky 的 `top` 一起放大而且**重新排版** |
| 提高對比：`--text-tertiary` / `--text-muted` **在已加深的基準上再提一階**，框線加深 | 投影機的黑階其實是灰的、會議室還開著燈，桌機上刻意的灰階層次投出去會直接消失 |
| 收起寫入型操作（`新增需求`、`Excel 匯出／匯入`、模擬帳號） | 台上沒有人會現場改資料，而**匯入會 TRUNCATE 整張表**，這種鈕不該出現在投影畫面上。搜尋／篩選／排序全部保留 |
| 資料列加斑馬紋（`--bg-row-zebra`，只在投影模式） | 投影對比低，一列橫掃到最右邊容易跳行。桌機不加 —— 會跟「Done 淡底色」互相干擾 |
| 進入時**借用**精簡模式與淺色底 | 16 欄放大後一定橫向捲；深色底在開著燈的會議室會糊成一片 |

- **「借用」不是接管**：進入時記下當下的深淺色／精簡模式／逾期優先，**離開時原封不動還原**。
  重新整理過（`ref` 已空）則維持現狀不亂還原 —— 這三個開關本來就各自記在 `localStorage`。
- ⚠️ **`zoom` 只掛在 `header` 與 `main`，不可掛到最外層**：`min-h-screen` 的 `100vh`
  不會被 `zoom` 縮放，掛在外層會變成 1.5 個螢幕高，永遠掛著一條沒有內容的垂直捲軸。
- ⚠️ **不可改用 `transform: scale()`**：它只是把畫好的畫面拉大，版面寬度沒變，
  右側欄位照樣被切掉而且捲不到。
- 兩層表頭的 sticky 在 `zoom` 下仍然正確 —— `headOffsets` 量到的是未縮放的 CSS px，
  套用時與表頭一起被同一個倍率放大，所以**不需要**為投影模式另外校正。
- 實測（精簡模式）：1920×1080 在 125~200% 全部無橫向捲動；
  1280×800 到 175% 無捲動、200% 溢出 38px（往下降一級即可，倍率鈕的用途就是這個）。
- `1_EMS規格確認`、`2_MSD確認中`、`3_MSD開發中`、`4_EMS驗收` 這四個日期欄，**若被異動過要在欄位上加一個標記符號**。
- 資料列上**只留最新的日期**，舊值一律進 detail 的異動紀錄。
- **不顯示「建立日」**：`CreatedAt` 與 `YearMonth` 是同一個日期、只是格式不同，資料列上放 `年月` 即可，完整建立時間放 detail。
- **一般模式不顯示「現況描述」**（2026-08-17 使用者調整）：內容常常是多行長文，塞進資料列只會被截成
  「1.因CMS WL…」反而看不出重點，改為只在 detail 完整顯示。
  **2026-08-19 起精簡模式例外**：那個模式只有 9 欄、有空間讓它換行完整顯示（見上方精簡模式段落），
  當初「會被截斷」的理由在那裡不成立。一般模式維持不顯示。

### 點選資料列展開的明細

- 需求補充（`Remark`）
- **完整專案時程**
  - ① EMS規格確認：`YYYY/MM/DD ~ YYYY/MM/DD`
  - ② MSD確認中：`YYYY/MM/DD`
  - ③ MSD開發中：`YYYY/MM/DD ~ YYYY/MM/DD`
  - ④ EMS驗收：`YYYY/MM/DD ~ YYYY/MM/DD`
- **異動紀錄**：四個階段各自的異動歷程
- **建立時間**（`CreatedAt` 完整時間）與最後更新時間
- **現況描述**（`StatusDesc`）完整內容，保留換行

---

## 情況一：新增需求

**必填**（缺一不可儲存）：`NID`、`MainCat`、`SubCat`、`EMS`、`1_EMSStart`、`1_EMSEnd`

- 詢問「是否直接壓日期」→ 選「是」時出現 Start / End 下拉，**兩欄都必填**否則不允許儲存。
  - Start → `1_EMSStart`
  - End → `1_EMSEnd`
- `NID` 重複時跳視窗提示。

**選填**：`Remark`、`MSD`

**自動產生的預設值**：

| 欄位 | 預設 |
|---|---|
| `OverallStatus` | `init` |
| `StatusID` | `1` |
| `RegDate` | 當天（`YYYY/MM/DD`），前端唯讀顯示，後端沒收到也會補今天 |
| `YearMonth` | 由 `RegDate` 反推的 `YYYY/MM` |

---

## 情況二：編輯需求

- 各欄位皆可編輯。
- 四個階段中**已壓過日期的，必須先解鎖才能修改**。
- **還沒填過的階段，要等前置階段填完才開放**（見上方「階段順序 gating」）。
- 修改日期**必須留下理由才能儲存**。
- 資料列只呈現最新值；舊值寫入 detail 的異動紀錄供追蹤。

---

## 情況三：刪除需求

**軟刪除** — 資料庫保留紀錄，不做實體刪除。

---

## 與現行實作的落差（待處理）

### ✅ 已於 2026-08-17 完成（`05_statusid_and_softdelete.sql`）

| 項目 | 處理 |
|---|---|
| `StatusID` | DB 值由 `(1)`~`(5)` 正規化為 `1`~`5`，後端 `NormalizeStageCode()` 把任何括號寫法都收斂成純數字；資料列與編輯視窗都加回此欄 |
| 刪除 | 改軟刪除。新增 `IsDeleted BIT NOT NULL DEFAULT 0` / `DeletedAt DATETIME2(0)`，查詢與匯出皆帶 `WHERE IsDeleted = 0` |
| NID 唯一性 | 新增與編輯都在後端檢查，重複回 `409` + 訊息，前端跳視窗提示 |
| 新增必填 | `NID` / `MainCat` / `SubCat` / `EMS` / `1_EMSStart` / `1_EMSEnd` 前後端都擋 |
| 資料列欄位 | 移除「建立日」與「現況描述」（都改放 detail），補上 `StatusID`、`MSD確認需求`，順序依上方清單。DOM 共 15 欄 |
| `2_MSDHistory` | DB 早有 `MsdConfirmHistory` 欄但 API 沒讀寫，本次補齊 |
| 編輯視窗四階段 | `Confirm EMS Spec Date` 從「MSD 開發」搬出來自成 `2. MSD 確認Spec`，原本的 2/3 順延為 `3. MSD 開發` / `4. EMS 驗收`。② 改日期會寫進 `2_MSDHistory` 並強制填理由 |
| `YearMonth` 格式 | `06_normalize_yearmonth.sql` 把既有的 `26/Dec` / `Jul/26` 等 27 筆全部正規化為 `YYYY/MM`；`FormatYearMonth()` 改為支援英文月份縮寫且兩段順序可互換，並套用在所有寫入路徑 |
| `Remark` 呈現 | 明細與資料列都不再做成超連結（原本 `<a href>` 會把描述文字當網址）；編輯視窗欄位改名為「需求補充 (Remark)」並改成 textarea |
| ② 移除備註欄 | 移除 `Confirm 備註` 輸入欄，該階段只壓確認日期 |
| `End ≥ Start` | ①③④ 三個區間前後端都驗證，並在 `<input type="date">` 加 `min` / `max` |

### ✅ 已於 2026-08-18 完成（第 11 批 / `07_add_regdate.sql`）

| 項目 | 處理 |
|---|---|
| 年月 → 註冊日期 | 新增 `RegDate DATE`，資料列與明細改顯示 `YYYY/MM/DD`；`YearMonth` 降級為由 `RegDate` 反推的衍生值，只留給匯入匯出與趨勢圖 |
| StatusID 命名 | 資料列表頭 `ID` → `StatusID`，儲存格從純數字改為「代號 + 階段名稱」彩色 pill；名稱統一為 1.EMS規格確認 / 2.MSD確認中 / 3.MSD開發中 / 4.EMS驗收 / 5.結案 |
| 四階段顯示名稱 | 編輯視窗區塊標題、資料列表頭、明細時程、軌跡時間軸、到期預警、必填欄位提示全部改為 `N_XXX` 命名，且只動顯示文字不動 Excel／DB 欄名 |
| 欄位篩選 | StatusID 欄可用代號或階段名稱篩選；註冊日期欄照畫面的 `YYYY/MM/DD` 比對 |
| 匯出 | 新增 `RegDate` 欄（排在 `StatusID` 與 `YearMonth` 之間），匯入可回吃；沒有 `RegDate` 欄的舊檔則由 `YearMonth` 補 01 日 |

### ✅ 已於 2026-08-18 完成（第 12 批，純前端）

| 項目 | 處理 |
|---|---|
| 需求統計狀態 | 從總覽搬到明細表，改為**可點的統計卡**（全部／Init／Ongoing／Pending／Done + 件數），點一下即篩選、再點一次取消。數字是**連動的 facet**：已套用其他篩選但不含狀態本身，所以加總永遠等於下方表格筆數 |
| 人員篩選 | 工具列新增 EMS／MSD 下拉。選項**直接從資料裡取**（不是 Personnel 名單）——名單上有但資料裡沒有的人，選了只會得到空清單反而像壞掉。空負責人歸入「未指派」 |
| 逾期呈現 | 「到期預警」頁籤**移除**，改為工具列的「逾期」下拉（需關注／已逾期／7 日內到期）＋「逾期優先」排序。判定規則沿用 `buildDueList()`，未重寫 |
| Done 置底 | 預設開啟，做成可關閉的 toggle（否則使用者點欄位排序時會以為排序壞掉）。判定為 `OverallStatus = Done` **或** `StatusID = 5` 任一成立 |
| 清除全部 | 一鍵清掉搜尋、狀態、EMS、MSD、逾期與所有欄位篩選；旁邊常駐「顯示 N / M 筆」 |
| 總覽 | 保留。移除「需求狀態分佈」卡（已搬走），風險預警改為整列寬。點其中一筆會切到明細表、套上「需關注」篩選並自動展開該列 |

### ✅ 已於 2026-08-18 完成（第 13 批 / `09_create_history_table.sql`）

| 項目 | 處理 |
|---|---|
| 稽核表 | 新增 `dbo.Controltable_History`，一列 = 一個階段的一次異動事件，含異動時間／人員／類型／原因分類／前後日期值／文字說明共 7 項。四個 `*History` NVARCHAR 欄位程式端不再讀寫（保留不刪） |
| 異動類型 | `init` / `日期異動` / `提早完成` / `延期完成` / `規格回退` 五種。**`init` 不算異動**，所有次數統計一律排除 |
| init 基準 | 新增需求與 Excel 匯入時，已填的日期都會寫入一筆 `init`，讓每筆資料都有起始基準可對照 |
| 異動原因分類 | 解鎖改日期時必須先選 `規格變更` / `優先級調整` / `技術問題` / `其他`，再填文字說明，兩者缺一不可儲存 |
| 異動人員 | Windows 驗證（`Microsoft.AspNetCore.Authentication.Negotiate`，作法對齊 `C:\Gantt`）。前端載入時打 `/api/whoami` 取得帳號後隨寫入附帶；取不到時稽核紀錄的 `ChangedBy` 留空，**不擋存檔** |
| 模擬帳號 | 開發環境（`Auth:AllowSimulation=true`）可從標題列切換模擬帳號測試。模擬寫入的稽核列 `ChangedBySource='simulated'`，畫面上標「（模擬）」，**不會冒充真實登入者** |
| 軌跡呈現 | 明細的時間軸與編輯視窗的異動紀錄都改讀稽核表。前後值直接來自欄位，不再用「下一筆的原日期」反推 |

### ✅ 已於 2026-08-18 完成（第 14 批，純前端 + 後端驗證，未動 DB）

| 項目 | 處理 |
|---|---|
| 階段順序 gating | 前置階段填完才開放下一階段「從空白開始填寫」。`PHASES` 加 `gate`，前端 `isPhaseOpen()` / `fieldLockReason()`（回 `'gated'` / `'locked'` / `null`），後端 `PhaseGatingViolations(req, before)` 在 POST / PUT 都擋回 `400` |
| 兩種鎖分家 | 灰色實心 `GateLock`（前置未完成，不可解，hover 顯示「請先完成 XX 的日期」）與原本的線條解鎖鎖頭（已有值防誤改，可解）分開呈現 |
| 存檔前再擋一次 | 日期欄雖已 disable，但「先填 ③ 再把 ② 清掉」這種倒著改的順序會漏過去，`handleSave` 補上同一條規則 |
| 400 視窗標題 | 從寫死的「必填欄位未完成」改為中性的「無法儲存」（400 現在還包含日期區間與階段順序） |

### ✅ 已於 2026-08-18 完成（第 15 批 / `10_add_actualend_and_counters.sql`）

| 項目 | 處理 |
|---|---|
| 實際完成日 | 新增 `SpecActualEnd` / `MsdConfirmActualEnd` / `MsdActualEnd` / `UatActualEnd`，**只有延期完成才寫入** |
| 計數欄 | 新增 `DelayCount` / `EarlyCount` / `RollbackCount`（denormalized，供第 17 批的徽章排序與篩選；`RollbackCount` 留給第 16 批寫入） |
| Done 推進 | 新端點 `POST /api/requirements/{id}/done`，依「今天 vs 原訂 End」判定提早或延期，推進 `StatusID` 並連動 `OverallStatus` |
| 重複標記 | 同一階段已標記過完成回 `409`（只看最後一次規格回退之後的紀錄） |
| 資料列呈現 | 延期完成的欄位顯示「原訂（灰）→ 實際（紅）」；明細的四階段與時間軸同步呈現，且延期的原訂日期不畫刪除線 |
| 逾期標示 | `StatusID` 已推過的階段不再標逾期（否則提早完成把 End 改成今天後，那格會冒出「今天到期」） |
| 匯出 | 新增 7 欄（`1_EMSActualEnd` / `2_MSDActualConfirm` / `3_MSDActualEnd` / `4_EMSActualEnd` / 三個計數）。**匯入刻意不吃**，避免 TRUNCATE 重灌時撞欄 |

### ✅ 已於 2026-08-18 完成（第 16 批，沒有新腳本）

| 項目 | 處理 |
|---|---|
| 規格回退 | 新端點 `POST /api/requirements/{id}/rollback`，可退回先前任一 `StatusID`，清空 ≥ 目標階段的全部日期與 `*ActualEnd` |
| 稽核快照 | 清空**之前**先為每個被清的階段寫一列 `ChangeType='規格回退'` / `ReasonCategory='規格變更'`，說明記「由 X 回退至 Y：<使用者輸入>」 |
| 計數 | `RollbackCount + 1`；三個計數欄都不清空 |
| 狀態連動 | `OverallStatus = Done` 時回到 `Ongoing`；`StatusID` 設為目標階段 |
| 重新鎖定 | 靠第 14 批的 gating 自動生效，未另外寫程式 |
| 重新完成 | 回退後該階段可再次標記完成（Done 的重複檢查只看最後一次回退之後的紀錄） |

### ✅ 已於 2026-08-18 完成（第 17 批，純前端）

| 項目 | 處理 |
|---|---|
| 警示徽章 | NID 欄下方顯示 `🔄N`（規格變更回退）與 `⏰N`（執行延期）兩個獨立標籤，`DelayCount >= 2` 轉紅；0 次不顯示 |
| 篩選 | 工具列「警示」下拉：有執行延期 / 延期 2 次以上 / 有規格回退（件數即時顯示） |
| 排序 | 「延期最多」「回退最多」兩個開關，依次數由多到少；用 `sortConfig` 實作所以與表頭排序互斥 |
| 版面 | 不新增欄位。實測 1366px 頁面與表格皆無捲動、1152px 頁面溢出 0（表格內捲 176px，與加徽章前相同） |

> 這批做完，2026-08-18 拍板的 7 批（第 11~17 批）**全部完成**。

### ⬜ 尚未實作

| 項目 | 現況 | 規格要求 |
|---|---|---|
| Next check 藍色標示 | 未實作（僅 `MsdConfirmNote` 一個自由文字欄） | 四個階段皆須支援，日期標藍 + 說明列在資料列上方 |
